# Gustav OS - STATUS

Sidst opdateret: 2026-07-05 (MAD TILPAS-STI: suggest_meal kan nu TILPASSE en gemt ret til Gustavs faktiske indkoeb ('tilpas den bagte ret, jeg har 800g oksekoed 12%', 'skalér til 6 portioner'). ARKITEKTUR (LLM i kanten, determinisme i kernen): LLM tagger ingrediens-roller (protein/carb/fat/base) i ÉT kald, KODEN loeser makro-reglen DETERMINISTISK via fixed-point der skalerer kulhydrat- og fedt-leddet mod macroTargets(protein), holder protein-kilden fast, saetter kcal=4P+4C+9F. Generator-smoke bekraeftede konvergens (10% koed / skalering / magert koed -> carb:fedt ~1,33, fedt ~30%, protein ~200/3000); intrinsisk fedt koed (15%) faar aerlig diagnostisk advarsel i stedet for loegn. Ny 'Bagt oksekoedsrisotto al forno' (800g, designet via multi-agent workflow) lagt i kataloget. Router + mirror wired til tilpas-fraser. Kode committes nu. TIDLIGERE 2026-07-05 (MAD MAKRO-REGEL v2 + SKYR UD: generator bruger nu 'protein FOERST ~200 g/3000 kcal, saa kulhydrat:fedt = 4:3' med KONKRETE gram-maal (3000 kcal = 200 P / 310 C / 103 F) + fedt-gulv ~30% + kcal-konsistens-krav; generator-smoke bekraeftede carb:fedt gik 1,8-2,0 -> 1,33 og fedt -> ~30%. Seed-hoensesuppe: skyr fjernet paa Gustavs oenske, skaleret til 3000/200/310/103. Kode committes nu (seed-data allerede i prod-DB). TIDLIGERE 2026-06-29 (MAD-MODUL RECALL-FIX: 'har du opskriften paa X' rammer nu suggest_meal (ikke search_memory), og suggest_meal slaar op i kataloget paa navn FOER den genererer; seed-opskrift skaleret til 1,35 kg kylling - committes nu. TIDLIGERE 2026-06-27: MAD/HELBREDS-MODUL fase 1 LIVE (recipes + suggest_meal, committet 0785926 + pushet). MAKRO-OPGRADERING: generator sigter nu energi-split 40/30/30 + hoejt protein (ikke fast 3000/200), recipes faar carbs_g/fat_g via migration 0018, Telegram-svar viser makro-split - committes nu. 3 kost-praeferencer gemt i memory_facts (gryde/one-pot, ingen linser, raa fedt) styrer forslag. RLS-hul paa public.memory_search LUKKET i prod. Se de oeverste Faerdigt-bullets. TIDLIGERE 2026-06-05: BRUGERDEFINEREDE SYNDER (Gustav kan tilfoeje egne synder ligesom kategorier; AI bruger dem ogsaa) KODET + VERIFICERET LIVE mod lokal dev, IKKE committet - afventer commit-ja, se øverste Færdigt-bullet. TIDLIGERE samme dag: PRIVATLIVS-SLØR (forside + finans øje-toggle slører nettoformue/synder; delt localStorage-state, no-flash, synergistisk) KODET + VERIFICERET LIVE mod lokal dev, IKKE committet - afventer commit-ja, se øverste Færdigt-bullet. TIDLIGERE samme dag: OPGAVE-ASSISTENT-INTEGRATION (Telegram-routeren kan nu oprette/afkrydse/flytte/slette/liste opgaver - 5 nye tools, deterministisk hint-match, kontrakt: actionable "husk at" -> opgave) KODET+VERIFICERET, IKKE committet (afventer commit-ja) - se øverste Færdigt-bullet. TIDLIGERE samme dag: UI-POLISH committet 62a05ef + pushet (graf m. akser kr+DD/MM/YYYY, grøn op/rød ned, nav-aktiv-fane fixet via klient-usePathname, Dette år-swing, fuldt navn) + PRODUKTIVITETS-MODUL kodet+verificeret mod prod (migration 0016 kørt; cron gav produktivitet 88,3% / dækning 39,4% på rigtige data), IKKE committet endnu - afventer commit-ja. TIDLIGERE 2026-06-04: Modul 1+2+3 LIVE, committet + pushet: Modul 2+3 = commit 32b0337, Vercel auto-deployede. Modul 4 FINANS LIVE (committet + pushet). Migration 0013 KØRT i prod. Rigtige data importeret af agenten: 3.270 bank-posteringer + 1.079 Storebox-kvitteringer (132 matchet, 736 varelinjer), ALT AI-klassificeret. Nettoformue 1.223,70 kr, synder juni: takeaway 675 / spil 310 / sodavand 70 / snacks 21. tsc + lint + next build rene. Dev kører på :3000. SPØRGE-LOOP (web) nu OGSÅ bygget + verificeret end-to-end: review-kø på /finans (363 'andet' + fremtidige lav-konfidens), din rettelse læres som regel (scope finance) + anvendes retroaktivt på lignende usikre posteringer. Spørge-loop committet (a99b99e). MÅNEDLIG IMPORT nu selvbetjent: upload bank-CSV + Storebox på /finans (idempotent) + ny cron `/api/cron/finance-classify` (pg_cron migration 0014, hvert kvarter, KØRT i prod) kategoriserer de nye posteringer i baggrunden, + kort Telegram-opsummering ved import. Bygget + committet (97317cb) + pushet; pg_cron -> pg_net -> prod VERIFICERET end-to-end (20:30-fyring gav HTTP 200). MODUL 5 UGENTLIG REVIEW nu KODET + VERIFICERET mod prod (migration 0015 kørt; dry-run + fuld cron-sti via lokal dev: tid hentet i runtime, gemt, second-brain-indekseret, leveret på Telegram), committet (d367c75) + pushet + deployet (cron-route live, denne uges review allerede genereret + leveret). NETTOFORMUE-KURVEN (net_worth_snapshots) nu OGSÅ bygget + verificeret (510 daglige snapshots backfillet fra bank-historikken 2024-05 -> nu; backfill efter import + dagligt snapshot i finans-cron; kurve på /finans + sparkline på forsiden), committet 3b7d84a + pushet + deployet. Hele byggeplanen (Modul 1-5) komplet + LIVE, og siden udvidet 2026-06-05 med UI-polish + produktivitets-modul - se nyeste bullet.)

## Hvor er vi
Projektet er komplet og LIVE: hele Modul 1-5-redesignet (forside, opgaver, mål, finans, review) + produktivitets-modulet kører på prod, alt committet + pushet (seneste commit 2e8711f, 2026-06-05); ENESTE ucommittede er dagens opgave-assistent-integration (se øverste Færdigt-bullet, afventer commit-ja). De oprindelige 7 faser plus dashboard v2 ligger under. Proaktive briefinger er nu skåret ned til ÉT ugentligt mønster-flag (morgen- og aftenbrief afskaffet 2026-06-03 - de havde ingen reel funktion). Hele systemet kører på `https://gustav-os.vercel.app` med Telegram-webhook, magic link-auth, automatisk kalender-write med Telegram-veto, semantisk søgning i second brain, balance-rapport, captures-filtrering + søgning, web-capture og tids-tracking via Telegram. Vercel Hobby blokerer fortsat sub-daglig cron. LØST 2026-06-01: Supabase pg_cron + pg_net kalder `/api/cron/actions` hvert 10. minut direkte fra databasen (migration `0007_pg_cron_actions.sql`). CRON_SECRET ligger krypteret i Supabase Vault (`gustav_os_cron_secret`), aldrig hos tredjepart. Verificeret: pg_cron-jobbet `gustav-os-actions` er aktivt og endpointet svarer `{"ok":true}`. 17-timers-lagget er væk. Vercel daily cron (`0 4 * * *`) bevaret som fallback. GitHub Actions schedule blev forsøgt 2026-05-31 men `schedule`-events fyrede aldrig (manuel dispatch virkede); droppet til fordel for pg_cron.

## Færdigt
- NYT - MAD-MODUL: TILPAS-STI (skalér/juster en gemt ret til faktisk indkoeb) 2026-07-05 (kodet + tsc/lint + router-regression + generator-smoke rene; kode committes nu). Gustav bad om at kunne sige "jeg fandt X paa tilbud, ret opskriften til" og faa makro-korrekte maengder. LOESNING = laerebogs-hybrid (LLM i kanten, determinisme i kernen): (1) ROUTING: suggest_meal-beskrivelse + constraints + ny router-regel (spejlet i `scripts/agent-router.mjs`) saa "tilpas den bagte ret, jeg har 800g oksekoed 12%" / "skalér hoensesuppen til 6 portioner" rammer suggest_meal med baade rettens navn OG maengderne i constraints. (2) DETEKTION i `suggestMeal` (`lib/meals.ts`): naar constraints matcher en gemt ret OG (`ADAPT_INTENT` = tilpas/juster/skaler/omregn ELLER `HAS_AMOUNT` = tal+g/kg) -> `adaptRecipe()` i stedet for at returnere skabelonen ordret. (3) `adaptRecipe`: ÉT LLM-kald (`callAdapt`, temp 0.3) tagger hver ingrediens' ROLLE (protein/carb/fat/base) + makroer pr. linje; derefter loeser KODEN reglen DETERMINISTISK i et fixed-point: skalerer kulhydrat-leddet (ris) og fedt-leddet (ost) mod `macroTargets(P)` (= ren formel: kcal=15P, kulhydrat=11P/7, fedt=33P/63, udledt af 200g/3000kcal + 4:3), holder protein-leddet fast, gentager til stabilt (svag kobling -> konvergerer). kcal saettes af koden (4P+4C+9F) -> label-drift umulig. `scaleLine` skalerer maengde-tallet i hver linje (dansk komma). `formatReply` viser '(tilpasset)' + note. VIGTIGT: adapterede versioner GEMMES IKKE i kataloget (engangs-tilpasning). VERIFICERET (smoke mod rigtige Haiku): 1kg 10% -> carb:fedt 1,36 / fedt 31% / protein 199@3000 KONVERGERET; skalér til 6 -> 1,34 / 31% / 202; 500g 5% -> 1,33 / 31% / 202; 1kg 15% (intrinsisk for fedt) -> fjerner al ost + AERLIG diagnostisk note ('fedt hoejt - draen fra/vaelg magrere'), konvergerer bevidst ikke. Router-regression: tilpas/skalér-fraser -> suggest_meal, recall -> suggest_meal, praeference -> save_memory, biokemi -> search_memory (nul regression). KENDT SVAGHED: meget magert koed (5%) faar koden til at skrue osten kraftigt op for at naa fedt-gulvet -> ost-tung/stoerre ret (aegte makro-tradeoff, note viser tallene); kan forfines med olie som renere fedt-haandtag. NAESTE: live Telegram-test efter deploy ('tilpas den bagte oksekoedsret, jeg har 1 kg oksekoed 10%').
- NYT - MAD-MODUL: MAKRO-REGEL v2 + SKYR UD 2026-07-05 (kodet + tsc/lint + generator-smoke rene; seed-data allerede opdateret i prod-DB, kode committes nu). (1) SEED-HOENSESUPPE: skyr fjernet paa Gustavs oenske (han bruger den ikke); INGEN kompensation - kan ikke tilfoeje mere kylling (én hel 1,35 kg fugl m. knogler der pilles fra), retten skal ikke vaere cremet. Skaleret til 3000 kcal / 200 g protein / 310 g kulhydrat / 103 g fedt (ris 240->310 g toer, olie 1,5->2,5 spsk raa), 3 portioner. (2) MAKRO-REGEL PRAECISERET (Gustavs prioritetsordre, erstatter det gamle 40/30/30): protein FOERST (~200 g pr. 3000 kcal = ~27% af energi), DEREFTER resten fordelt kulhydrat:fedt = 4:3 (fra 40/30-splittet). Grund: 40/30/30 = 30% protein overproducerede (~220 g ved 3000 kcal); Gustav vil ramme 200 g foerst, saa fordele fedt/kulhydrat uden at fedtet forsvinder. (3) GENERATOR (`lib/meals.ts`, 3 steder: header-komm., system-prompt, `krav`-felt) skrevet om fra 40/30/30-split til KONKRETE gram-maal (referencedag 3000 kcal = 200 P / 310 C / 103 F, skaler samlet) + FEDT-GULV ~30% af kcal + KONSISTENS-KRAV (kcal = 4P+4C+9F). memory_facts BEVIDST IKKE brugt: makro-reglen er hardcoded i prompten, en fact oveni = to kilder der driver. VERIFICERET: tsc+lint rene; generator-smoke (engangs-script, 3 varianter) FOER fix gav carb:fedt 1,8-2,0 + fedtfattigt (67-90 g) + label-drift -13% (kcal-label 3000 men makro-sum ~2600); EFTER fix carb:fedt 1,33-1,35 + fedt 29-31% + protein ~200/3000 + drift <5% (fiske-variant skoed protein 231 g, acceptabelt - torsk er magert, fedt-gulvet holdt). NAESTE: commit efter Gustavs ja -> live Telegram-test ('foreslaa noget nyt til aftensmad' -> skal ramme ~200 g protein + 4:3 kulhydrat:fedt).
- NYT - MAD-MODUL: RECALL-FIX + SKALER-NED 2026-06-29 (kodet + tsc/lint + router-regression rene; committes nu). BUG fundet via `telegram_updates` (nyeste = reason 'recall_answered'): Gustav spurgte til den gemte hoensesuppe, men routeren sendte beskeden til search_memory (second brain), hvor opskrifter IKKE ligger -> 'den havde den ikke'. TO aarsager fixet: (1) ROUTING: suggest_meal-beskrivelse + ny router-regel saa 'har du opskriften paa X', 'vis den X', 'hvad var den ret du foreslog' nu rammer suggest_meal (ikke search_memory). Spejlet i `scripts/agent-router.mjs`. (2) LOGIK (`lib/meals.ts`): suggest_meal genererede ALTID naar der var constraints; nu slaar den FOERST op i kataloget paa navn (ny `findCatalogByText` + Danish-aware `normalizeTitle` med aa/ae/oe-mapping, saa 'hønsesuppe' matcher gemt 'hoensesuppe') og returnerer den GEMTE opskrift hvis et titel/tag-token (len>=5) matcher; NEW_INTENT-ord ('ny','nyt','overrask'...) tvinger generering. VERIFICERET: router-regression 7 fraser (recall-opskrift -> suggest_meal x3, biokemi -> search_memory, opgaver -> list_tasks, svinekoed -> save_memory, ALLE korrekte, nul regression); match-smoke-test (hoensesuppe-varianter MATCH, 'oksekoed tikka masala'/'noget hurtigt' no-match -> genererer). SEED-opskrift SKALERET til Gustavs 1,35 kg kylling (~3 portioner, 2820 kcal / 222 g protein, split 38/31/29). NAESTE: live Telegram-test efter deploy ('har du opskriften paa hoensesuppe' -> skal nu give den gemte ret).
- NYT - MAD-MODUL: MAKRO-OPGRADERING + KOST-PRAEFERENCER 2026-06-27 (kodet + tsc/lint rene; recipe-data + praeferencer ALLEREDE live i prod-DB/memory_facts, kode committes nu). Efter Gustavs feedback paa foerste opskrift: (1) GENERATOR (`lib/meals.ts`) skiftet fra '3000/200-ratio' til at sigte ENERGI-SPLIT ca. 40% kulhydrat / 30% protein / 30% fedt med protein hoejt i gram; laaser ikke totalen - maa lave stoerre batches (fx hel kylling) der giver flere maaltider. (2) Migration `0018_meals_macros.sql` (KOERT i prod): recipes faar carbs_g + fat_g, saa hele makro-splittet gemmes; Recipe-type/rowToRecipe/saveRecipe/generator udvidet; Telegram-svar viser nu split + pr-maaltid. (3) KOST-PRAEFERENCER gemt som globale memory_facts (laeses af generator via recallGlobal, gaelder ogsaa router): `mad-praeference-gryderet` (vil have gryde/one-pot hvor alt er blandet, ikke protein+tilbehoer-tallerkner), `mad-undgaa` (ingen linser), `mad-tilberedning-fedt` (fedt tilsaettes RAAT til sidst, opvarm ikke olivenolie). SEED-opskriften itereret med Gustav til en proteinrig one-pot hoensesuppe (hel kylling kogt, koed shreddet tilbage i fonden, ris kogt i fonden, skyr roert i, raa olie til sidst) - ~3550 kcal / 280 g protein / split 38-32-29 / 4 portioner. VERIFICERET: tsc exit 0, lint exit 0; makro-split tjekket i SQL paa hver iteration. NAESTE: live Telegram-test af generatoren efter dette deploy (fx 'foreslaa en gryderet til aftensmad').
- NYT - MAD/HELBREDS-MODUL fase 1 2026-06-27 (committet 0785926 + pushet + deployet). Foerste helbreds-segment: assistenten kan nu foreslaa KONKRETE opskrifter paa Telegram. EN verb `suggest_meal` i `lib/agent-router.ts` (spejlet i `scripts/agent-router.mjs`) -> ny `handleAgentSuggestMeal` i `lib/telegram-webhook.ts` -> nyt domaene-modul `lib/meals.ts` (`suggestMeal()`). EN tabel `recipes` (migration `0017_meals.sql`, KOERT i prod via apply_migration): voksende katalog, RLS deny-all som resten. LOGIK: uden konkret oenske + noget i kataloget -> genbrug mindst nyligt foreslaaet (variation via `last_suggested_at`); ellers generer via Haiku (claude-haiku-4-5) constrained af makro-kravet + Gustavs laerte kost-fakta (recallGlobal/memory_facts scope global) og gem tilbage i kataloget (best-effort, dedup paa normaliseret titel). MAKRO-KRAV (Gustavs valg 2026-06-27): hver ret proteintung, ratio ca. 3000 kcal / 200 g protein for HELE retten (~15 kcal pr. g protein); kcal+protein_g gemmes pr. ret. BEVIDST UDE (anti-over-engineering, jf. designet): ingen meal_log, ingen embedding/semantisk soegning, ingen pantry-tabel (kan senere udledes af storebox_receipts), intet dashboard/cron endnu. read-mostly -> samme double-gate-undtagelse som search_memory (eneste skrivning = save-back). DISAMBIGUATION: 2 nye router-regler ('et mad-spoergsmaal er ALDRIG search_memory'; varig kost-praeference/allergi -> save_memory). Diaet-fakta lagres scope='global' (save_memory-default), saa de baade rammer router-prompten og laeses af suggestMeal. SEED: Gustavs helstegte kylling lagt i kataloget (source manual, 3000 kcal / 250 g protein). VERIFICERET: tsc exit 0, lint exit 0; `node scripts/agent-router.mjs` ruter 'hvad foreslaar du til aftensmad' -> suggest_meal, 'jeg spiser ikke svinekoed' -> save_memory, 'find opskrift med kylling hurtigt' -> suggest_meal m. constraints, 'hvad sagde jeg om biokemi' -> search_memory; generator-smoke-test (midlertidigt script, slettet) gav parseable JSON, 3050 kcal / 205 g (ratio 14,9), respekterede 'ingen svinekoed'. IKKE testet: live Telegram end-to-end (kraever deploy; `USE_AGENT_ROUTER=1` er allerede live i prod). NAESTE: commit efter Gustavs ja -> push auto-deployer -> live Telegram-test ('hvad foreslaar du til aftensmad'). Senere: soevn-kobling (daily_logs), blodprover (health_observations + log_health-verb m. double-gate), storebox-grounding (soft hint), ugentlig madplan/aften-nudge via cron.
- NYT - SIKKERHED 2026-06-27: RLS AKTIVERET paa `public.memory_search` (var DEAKTIVERET = eksponeret for anon-noeglen; Supabase-advisor markerede det kritisk). Tabellen er en rest (0 raekker, ingen kode-refs ud over selve MCP-toolnavnet 'memory_search' i scripts/memory-mcp-server.mjs). Deny-all (RLS uden policies), samme moenster som resten; service_role uaendret -> ingen funktionel paavirkning. Anvendt direkte i prod via apply_migration (`enable_rls_memory_search`). INGEN repo-fil for denne (ren prod-DDL paa en rest-tabel).
- NYT - FINANS BRUGERDEFINEREDE SYNDER 2026-06-05 (kodet + tsc/lint/next build rene, verificeret live i Chrome mod lokal dev, IKKE committet - afventer commit-ja). Spejler kategori-featuren: Gustav kan tilfoeje egne synder (sin_tags) ud over de 6 faste via ny "Synder"-sektion paa /finans. Gustavs valg: AI bruger dem OGSAA. LAGRING: memory_facts scope 'finance_sin' (key=noegle, content=label), INGEN migration (sin_tag er fri text). ARKITEKTUR: `SinTag` loesnet fra lukket union til `string` (runtime-valideret mod merged liste); ny `lib/finance-sins.ts` (loadSins/addSin/deleteSin, genbruger slugifyCategoryKey fra finance-categories); `SinDef`-type + `SinSummary.label` tilfoejet i finance-shared. AI (`finance-classify.ts`): `buildTxSystem(cats, sins)` udvidet + ny `buildLineSystem(sins)` -> egne synder appendes til BEGGE Haiku-prompter (transaktioner + varelinjer); `coerce` validerer AI's sin mod `validSinKeys` (faste+egne) i stedet for isSinTag; byte-identisk uden egne synder (nul adfaerdsaendring nu). Classifier loader loadSins pr. koersel (baade tx- og linje-stien). `getSinSummary` taeller nu ENHVER ikke-tom sin_tag (ikke kun de faste via isSinTag) + resolver labels via loadSins -> custom synder kommer paa synde-kortet. `parseFinanceRule` validerer ikke laengere sin mod faste (men 'ingen'-sentinel bevaret) saa custom-forretningsregler med synd parses. Dominant-sin i merchant/produkt-grupper bruger raa vaerdi (`domSin || null`). De 4 sin-settere i actions validerer via ny `resolveSin` (loadSins) i stedet for isSinTag. ACTIONS: nye `addSinAction`/`deleteSinAction`. UI: ny `components/FinanceSins.tsx` (chips + tilfoej/slet, genbruger .fcat*-CSS); `sins: SinDef[]`-prop til FinanceMerchantReview/-Lines/FinanceTransactions (sin-selects + pille-labels dynamiske via en sinLabel-Map); ny "Synder"-kort i page.tsx (variabel `sinDefs` for ikke at kollidere med synde-summen `sins: SinSummary[]`). Front + finans synde-kort + weekly-review bruger nu `s.label` (fra SinSummary) i stedet for `SIN_LABEL[s.tag]`. STICKER: saetter Gustav en forretning/vare til en egen synd, lagres den paa transaktion/linje og kaskaderer som de faste. 12 filer (2 nye: finance-sins.ts + FinanceSins.tsx). VERIFICERET LIVE: Synder-kort viser 6 faste (ikke-sletbare), tilfoej "Testsynd" -> persisterede (memory_facts) + custom-chip med slet-kryds + i alle sin-dropdowns (efter de 6 faste), slet -> vaek alle steder; synde-summen viser stadig labels (Takeaway/Spil/Sodavand/Snacks) = INGEN regression fra label-aendringen. AI-integration deterministisk fra samme loadSins som UI'et (chip+dropdown bevist), prompt-build dækket af next build. NAESTE: commit efter Gustavs ja -> push auto-deployer; Gustav bruger det (tilfoej fx Tobak/Slik).
- NYT - FORSIDE/FINANS PRIVATLIVS-SLØR 2026-06-05 (committet 331a2db + pushet + deployet, verificeret live i Chrome; blur forstærket i opfølgning - se sidst i bulleten). Ny øje-toggle (Eye/EyeOff) der slører nettoformue + syndeudgifter, så Gustav kan vise skærmen uden at afsløre tal. Placeret TO steder: forsidens Finans-kort-header (ved siden af Live-tag'et) + /finans-headeren (ved siden af nettoformue-tallet). DELT STATE: klassen `fin-private` på `<html>`, gemt i localStorage (nøgle `fin-private`), så de to toggles er SYNERGISTISKE - trykker man den ene, følger den anden med ved navigation; på tværs af åbne faner synk via `storage`-event. NO-FLASH: inline-script i `layout.tsx` (første barn i `<body>`) sætter klassen FØR paint, så tal aldrig blinker frem når sløring var slået til; `<html suppressHydrationWarning>` dæmper className-mismatch (next-themes-mønsteret, ingen CSP der blokerer inline-script). KOMPONENT `components/PrivacyToggle.tsx` (klient, bruger `useSyncExternalStore` fordi staten ER en ekstern store - DOM-klasse + localStorage - ikke React-state; det undgår også eslint `react-hooks/set-state-in-effect`). CSS `globals.css`: `html.fin-private` slører `.nw-value / .swings .v / .nw-break / .nw-chart / .sin-total / .sin-rows / [data-private]` med `filter: blur(16px)` + 28px på det store headline-nettoformue-tal (labels + kort-titler forbliver skarpe; synde-rækker sløres HELT inkl. kategorinavne = mere privat, da lasterne selv er det følsomme). `.priv-toggle / .card-tools / .vh-r` styling. Kun ÉN ny JSX-markør krævet: `data-private` på /finans-headerens `.vh-sub` (den klasse genbruges på review/mål/opgaver, så kunne ikke selektor-blures globalt); alt andet rammes via finans-only klasser (grep-bekræftet unikke). FILER (5): ny PrivacyToggle.tsx + layout.tsx + page.tsx + finans/page.tsx + globals.css. FALDGRUBE FANGET under verifikation: Turbopack dev serverede STALE CSS fra `.next` (mine `.priv-toggle`-regler manglede selvom TSX var frisk -> filter blev `none` trods korrekt selektor-match) -> `rm -rf .next` + genstart løste det; frisk `next build` ren bagefter (så Vercel-deploy bliver ren). VERIFICERET LIVE i Chrome mod lokal dev (authed): no-flash (ls=1 -> siden loadede allerede sløret), toggle FRA/TIL (klasse + ls + filter + ikon/aria-pressed skifter), BIDIREKTIONEL synergi (slå FRA på /finans -> forsiden også FRA ved navigation, og omvendt), korrekte mål slørede mens "Finans"-titel skarp, knap 28×28 synlig ved Live-tag. OPFØLGNING (Gustavs test: "sløret ikke godt nok - kan tyde tallene"): blur øget 8px->16px + 28px på headline-tallet (8px skalerede ikke med skriftstørrelsen; et 52px-tal kræver ~halvdelen i blur for at blive ulæseligt). GOTCHA: Chrome understøtter IKKE max()/em inde i `blur()` (computer til 0px) -> rene px + separat headline-regel (samme specificitet, vinder via kilde-rækkefølge). De første `blur(0px)`-aflæsninger var transition-artefakter (`transition: filter .18s` læst ved t=0); bekræftet korrekt via no-flash-load = blur(28px) på headline / 16px resten. Forstærkningen committet + deployet. NÆSTE: Gustav bekræfter læsbarhed på prod.
- NYT - FINANS BRUGERDEFINEREDE KATEGORIER 2026-06-05 (kodet + tsc/lint/next build rene, committet + pushet + auto-deployet, verificeret live i Chrome). Gustav kan nu tilfoeje egne kategorier ud over de faste 10 via ny "Kategorier"-sektion paa /finans (tilfoej ved navn -> server slugger en noegle; slet kun egne). Gustavs valg: nye kategorier bruges OGSAA af AI'en (puttes i Haiku TX-prompten), ikke kun manuelt. LAGRING: memory_facts scope 'finance_cat' (key=noegle, content=visningsnavn), INGEN migration (transactions.category er fri `text` uden CHECK/enum, saa egne vaerdier gemmes direkte). ARKITEKTUR: `Category`-typen loesnet fra lukket union til `string` (valideres i RUNTIME mod merged liste, ikke i typen); `parseFinanceRule` validerer ikke laengere kategori mod de faste (saa custom-forretningsregler kan parses); ny `lib/finance-categories.ts` (loadCategories = faste+egne, addCategory med slugifyCategoryKey -> [a-z]-noegle med oe/ae/aa-mapping + dublet-tjek, deleteCategory kun egne); `memory-facts.ts` ny `deleteMemory(scope,key)`. CLASSIFIER (`finance-classify.ts`): `TX_SYSTEM` -> `TX_SYSTEM_BASE` + `buildTxSystem(custom)` der appender egne kategorier til prompten (BYTE-identisk naar ingen egne -> nul adfaerdsaendring nu); `coerce` validerer AI-svar mod et `validKeys`-Set i stedet for `isCategory`; varelinjer bruger fortsat kun faste noegler. ACTIONS: nye `addCategoryAction`/`deleteCategoryAction`; de 4 kategori-settere validerer nu via `resolveCat` (loadCategories) i stedet for `isCategory`; merchant-Faerdig-beskeden bruger label. `finance.ts`: dominerende kategori i listMerchantGroups/getMerchantLineGroups bruger nu raa vaerdi (`domCat || null`) saa custom ikke nulstilles til blank. UI: ny `components/FinanceCategories.tsx` (chips + tilfoej/slet, X paa egne); `categories`-prop sendt til FinanceMerchantReview/-Lines/FinanceTransactions (selects loader dynamisk); `page.tsx` loader loadCategories + render "Kategorier"-kort; `globals.css` `.fcat*`. STICKER: saetter Gustav en forretning til en egen kategori med Faerdig, gemmes en forretningsregel -> fremtidige posteringer fra samme forretning faar den automatisk. NUANCE: egen kategori faar kun NAVNET med i AI-prompten (ingen beskrivelse) -> AI-traefsikkerhed paa ukendte forretninger afhaenger af et sigende navn; forretningsregler er 100% praecise. 11 filer. NAESTE: Gustav bruger det (tilfoej fx "Gaver"/"Boern", sortér).
- NYT - FINANS VARE-GENNEMGANG (v2.1) 2026-06-05 (kodet + verificeret read-only mod prod, IKKE committet, afventer commit-ja). Fix efter Gustavs test af v2: udfoldningen viste hver KVITTERING, så han skulle åbne alle 60+ Netto-bonner for at rette fx cola. NU folder udfoldningen varelinjerne sammen PR. VARE (`productKey`): Netto -> 87 unikke varer (Coca Cola ×12, Pepsi Max ×10, Pågen Kanelgifler ×6...) sorteret efter hyppighed, med dominerende kategori/sin forudfyldt. Gustav retter en vare ÉN gang -> kaskaderer GLOBALT (alle butikker, Gustavs valg - en vare er den samme overalt). `lib/finance-shared.ts`: ny `productKey(text)` (HELT normaliseret varenavn, modsat merchantTokens første-ord) + `ProductGroup`-type. `lib/finance.ts`: `getMerchantLineGroups(token)` (henter forretningens tx-id'er via scan+filter pr. token, så deres varelinjer chunket via `.in('transaction_id',...)`, aggregerer pr. productKey med dominerende kat/sin + mixed, sorteret count dernæst |forbrug|) + `setProductLineCategory(key,cat,sin)` (GLOBAL: scanner ALLE transaction_lines, matcher productKey i kode, bulk-update category+sin_tag). ERSTATTEDE v2's `getMerchantTransactions`. `app/finans/actions.ts`: nye `getMerchantLineGroupsAction` + `setProductLineCategoryAction` (erstattede getMerchantTransactionsAction; `type Transaction`-import fjernet, `ProductGroup` tilføjet). Ny komponent `components/FinanceMerchantLines.tsx` (vare-rækker: navn, antal×, kategori- + sin-select, beløb; optimistisk override + global kaskade + "N varelinjer rettet"-note). `components/FinanceMerchantReview.tsx`: udfolder nu til `FinanceMerchantLines` (linesByToken lazy via getMerchantLineGroupsAction) i stedet for FinanceTransactions. `app/globals.css`: `.mlines`/`.mlines-hint`/`.mline*`. FinanceTransactions' redigerbare varelinjer (fra v2) er stadig i "E Posteringer" hvis en konkret bon skal ses. Synde-korrekthed uændret: `getSinSummary` læser varelinje-sin, så vare-rettelser slår igennem på synde-kortet. VERIFICERET: tsc + lint rene; read-only mod prod (Netto 87 varer, cola/pepsi=sodavand allerede tagget af AI; Wolt 0 varer = ingen Storebox-kvittering). BEGRÆNSNING: forretninger uden matchede kvitteringer (fx Wolt/DSB) har ingen varelinjer -> deres synd/kategori sættes på forretnings-niveau (Færdig), ikke pr. vare. PERF: getMerchantLineGroups = fuldt tx-scan + linje-fetch pr. udfold; setProductLineCategory = fuldt linje-scan pr. rettelse (lazy, ok ved personlig skala; ingen indeks muligt da nøglen udregnes i kode). FIX (farve/fortegn, 2026-06-05, 2 runder, commit 8b0fd93 deployet + verificeret live i Chrome): 1. forsøg gjorde varelinje-beløb NEUTRALE grå, men Gustav ville have FARVE med korrekt fortegn. Endelig fix: linjebeløb NEGERES ved visning (linjer er gemt omvendt af bank-niveau: vare = positiv kost, "Rabat" = negativ besparelse), så de matcher forretnings-totalen - udgift (cola/pant) = rød MED minus, Rabat = grøn UDEN minus. Gælder både FinanceMerchantLines (Netto-udfoldning) OG FinanceTransactions (E Posteringer). CSS: `.mline-amt.neg/.pos` + `.txln-amt.neg/.pos`. Verificeret live i Chrome: Rabat 327 kr grøn, Pant -69 kr rød, Coca Cola -211 kr rød, matcher Netto-total -43.631 kr rød. NÆSTE: commit efter Gustavs ja -> push auto-deployer -> Gustav kører gennemgangen (forretning -> fold ud -> ret varer -> Færdig).
- NYT - FINANS GENNEMGÅ PR. FORRETNING v2 2026-06-05 (kodet + verificeret read-only mod prod, IKKE committet, afventer commit-ja). Gustavs tre ønsker efter v1: (1) en forretning skal FORSVINDE når den er gennemgået, (2) fjern "Til gennemsyn"-kortet - den nye liste er den ENE review-flade (nye posteringer dukker op her), (3) redigér INDIVIDUELLE varelinjer (cola under Netto -> sodavand-synd; ellers er data forkert). "FÆRDIG"-SIGNAL = Model M: en forretning er færdig når ALLE dens posteringer har `category_source='manual'`. Kun forretnings-"Færdig" (`setMerchantCategoryAction` -> `applyLearnedCategory(token, cat, sin, {includeManual:true, source:'manual'})`) sætter HELE forretningen til manual; per-postering- og varelinje-rettelser gør det IKKE -> de fjerner ikke forretningen (honorerer Gustavs valg "kun Færdig fjerner"). Nye posteringer (cron-kilde 'rule'/'ai'/'storebox', aldrig 'manual') gør forretningen ikke-alle-manual -> den dukker op igen til gennemgang (præcis ønsket "nye posteringer kommer på listen"). INGEN migration (`category_source` fandtes på transactions). ÆNDRINGER: `lib/finance.ts` (`applyLearnedCategory` fik valgfri `source`-param; `listMerchantGroups({includeReviewed})` selecter nu `category_source`, udregner `reviewed = count>0 && manualCount===count` + filtrerer fra medmindre includeReviewed; ny `getMerchantTransactions(token,{limit=60})` = samme scan+filter-pr-token som applyLearnedCategory, sorterer kvitteringer-først så nyeste, cap 60; ny `setTransactionLineCategory(lineId,cat,sin)` der KUN rører transaction_lines.category+sin_tag - tabellen har ingen updated_at/category_source/status). `lib/finance-shared.ts`: `MerchantGroup.reviewed:boolean`. `app/finans/actions.ts`: ny `setLineCategoryAction` (KRITISK: kalder ALDRIG saveFinanceRule/applyLearnedCategory - en varelinje er ét køb, ikke en regel; ellers ville cola-tagging markere forretningen færdig), ny `getMerchantTransactionsAction` (read-only lazy), `setMerchantCategoryAction` cascader nu med source:'manual' + besked "Markeret færdig...". `components/FinanceTransactions.tsx`: varelinjer er nu REDIGERBARE (kategori- + sin-`<select>` pr. linje -> setLineCategoryAction, optimistisk patch af linesById) + `catOverride` så per-postering-kategori ikke snapper tilbage i udfoldningen (hvor items kommer fra en lazy klient-hentning der ikke gen-hentes ved router.refresh). `components/FinanceMerchantReview.tsx`: rækker er UDFOLDELIGE (chevron -> lazy `getMerchantTransactionsAction` -> genbruger `FinanceTransactions` for posteringer+varelinjer), "Gem"->"Færdig", ny "vis færdige"-toggle (default fra) + "færdig"-mærke. `app/finans/page.tsx`: "Til gennemsyn"-kort + `reviewItems`/`listReviewQueue`-import fjernet (funktionen beholdt - scripts bruger den), henter `listMerchantGroups({includeReviewed:true})`, badge tæller ikke-færdige. `app/globals.css`: `.txln` 4 kolonner + mobil, `.mrev-bar`/`.mrev-toggle`/`.mrev-drill`/`.pill.done`. SYNDE-KORREKTHED bekræftet: `getSinSummary` summerer BÅDE transaktions- OG varelinje-sin uden dobbelttælling (matchede posteringer bærer aldrig egen sin), så cola->sodavand slår korrekt igennem på synde-kortet. VERIFICERET: tsc + lint rene; read-only dump mod prod (473 forretninger, 0 færdige endnu, Netto 544x/DSB 389x/Wolt folder korrekt; `getMerchantTransactions('netto')` = 60 kvitteringer, KVT-først, nyeste-først, source='rule'). Manuel UI-test er auth-gated -> sker efter deploy. EDGE/scope: forretnings-Færdig sætter sin=null for hele forretningen (sikkert for matchede; kan nulstille en transaktions-sin på umatchede samme-token -> hint "tjek linjer før Færdig"); getMerchantTransactions cap 60 (ældre kvitteringer ud over 60 nås ikke i udfoldningen, men Færdig dækker kategori for alle); ingen ægte "fortryd færdig" (vis-færdige + gen-Færdig dækker rettelse). NÆSTE: commit efter Gustavs ja -> push auto-deployer -> Gustav kører gennemgangen live. Plan: `~/.claude/plans/fizzy-hopping-noodle.md`.
- NYT - FINANS: GENNEMGÅ PR. FORRETNING 2026-06-05 (committet + pushet + deployet). Løser at "sikre" fejl-klassificeringer (fx Hiper=takeaway, men er internet-abonnement) ikke kunne rettes: review-køen på /finans viste kun USIKRE (needs_review/andet/null), 40 ad gangen, så confident-men-forkerte var usynlige OG en rettelse nåede dem ikke. TO ÆNDRINGER. (1) KASKADEN UDVIDET: `applyLearnedCategory(token, cat, sin, {exceptId?, includeManual?})` i `lib/finance.ts` rammer nu ALLE posteringer med samme `merchantToken`, OGSÅ de sikkert klassificerede (før: `.or('status.eq.needs_review,category.eq.andet,category.is.null')` - kommentaren sagde eksplicit "Rører ALDRIG en sikkert klassificeret postering"; det var præcis begrænsningen Gustav ramte). Per-postering-rettelse (`setCategoryAction`) kalder med `includeManual:false` (beskytter Gustavs egne tidligere manuelle valg via `.neq('category_source','manual')` + exceptId); forretnings-rettelse kalder med `includeManual:true` (autoritativ for hele forretningen). (2) NYT VIEW "Gennemgå pr. forretning" øverst på /finans: `listMerchantGroups()` (lib/finance.ts) paginerer ALLE posteringer og folder pr. merchantToken til {label=hyppigste tekst, count, total, spend=Σ|beløb|, dominerende category+sin, mixed, examples}, sorteret efter vægt (count, dernæst spend). `components/FinanceMerchantReview.tsx` (klient): én række pr. forretning, søgefelt, kategori- + sin-`<select>` + Gem-knap -> ny `setMerchantCategoryAction(token, cat, sin)` i `app/finans/actions.ts` (saveFinanceRule + applyLearnedCategory includeManual:true). `MerchantGroup`-type bor i `lib/finance-shared.ts` (klient-sikker). CSS `.mrev-*` + `.pill.warn` i globals.css. BEVIDST: INGEN AI-genklassificering (Gustavs krav: rør ikke HVORDAN posteringerne blev klassificeret, kun evnen til at rette + kaskade), INGEN migration, INGEN 40-kappe på det nye view. VERIFICERET: tsc + lint rene; `merchantToken`-gruppering enheds-testet rent uden DB (Hiper/HIPER A/S/Hiper Danmark -> `hiper`; NETTO 7103/Netto Odense -> `netto`; Wolt/WOLT.COM -> `wolt`). Selve kaskade-skrivningen sker når Gustav trykker Gem på det deployede site. NÆSTE: Gustav kører gennemgangen live (søg forretning -> sæt kategori/sin -> Gem kaskaderer til hele historikken); bagefter kan 40-kappen på "Til gennemsyn"-kortet fjernes hvis ønsket.
- NYT - ASSISTENT KAN NU STYRE OPGAVE-BOARDET 2026-06-05 (kodet + verificeret, IKKE committet, afventer commit-ja). Løste Gustavs egen board-opgave "Forbind assistent med opgaver-fanen i personalOS". Telegram-routerens tool-calling-lag (bag det allerede-live `USE_AGENT_ROUTER`-flag) fik 5 NYE tools i `lib/agent-router.ts` (spejlet i `scripts/agent-router.mjs`): `create_task`, `complete_task`, `move_task`, `delete_task`, `list_tasks`. Hver mapper i `dispatchViaAgent` (`lib/telegram-webhook.ts`) til de EKSISTERENDE mutators i `lib/tasks.ts` (createTaskFromCapture/completeTask/moveTask/deleteTask/listTasks) - INGEN ny infrastruktur, INGEN migration (tasks-tabellen findes fra 0011). `create_task` genbruger samme Haiku-klassificering som web-boardet. complete/move/delete identificerer opgaven ud fra et søgeord (`task_hint`): matchet vælges DETERMINISTISK i kode (nye helpers `resolveTaskByHint`/`taskMatchScore`/`normalizeTaskText` - eksakt > substreng > token-overlap), ALDRIG af LLM'en (jf. instans-lektien i AGENTS.md); tvetydigt -> assistenten spørger hvilken, intet match -> lister åbne opgaver. KONTRAKT-BESLUTNING (Gustav valgte 2026-06-05): (a) actionable "husk at GØRE X" -> opgave på boardet (ikke note); rene tanker/observationer/fakta -> note. create_task/save_note-beskrivelser + router-regler skåret skarpt så grænsen er forudsigelig. (b) "hvad skal jeg nå i dag" -> `list_tasks` (boardet), ikke `search_memory`. VERIFICERET: tsc + lint rene; router-valg testet på 10 sætninger (alle korrekte, inkl. grænsen "møde fredag kl 14" -> kalender vs "ring fredag" -> opgave); hint-matcher unit-testet (eksakt/substreng/case-insensitiv/tvetydig->spørg/intet->fortæl); golden-set regression (`scripts/replay.mjs`) 22 korrekte / 0 forkerte / 3 afklarende spørgsmål (`golden-set-labeling.csv` relabelet for de 5 rækker hvis korrekte label skiftede med kontrakten - CSV er gitignored/lokal, indeholder Gustavs rigtige beskeder). Browser-adgang: agenten kan nu se de indloggede sider direkte via Claude-in-Chrome (Gustavs forbundne Chrome), så forsiden + grafer kan verificeres visuelt. IKKE testet endnu: live Telegram end-to-end mod prod (kræver deploy; bot-flowet rører prod-DB + svarer i Gustavs chat). NÆSTE: commit efter Gustavs ja -> push auto-deployer -> live Telegram-test (opret + slet en throwaway-opgave).
- NYT - PRODUKTIVITETS-MODUL + UI-POLISH 2026-06-05. (1) UI-POLISH committet 62a05ef + pushet: `components/NetWorthCurve.tsx` lavet om fra makeshift-sparkline til RIGTIG graf med akser (y=kr, x=DD/MM/YYYY) + gridlines; grøn ved op-trend / rød ved ned (var lilla og blev ALDRIG grøn før); luft mellem graf og synder. Nav-aktiv-fanen (lilla boks) frøs på /finans fordi root-layoutet ikke re-renderer ved soft-navigation - aktiv-detektion flyttet til ny KLIENT-komponent `components/Tabs.tsx` med usePathname() (layout.tsx renderer nu bare <Tabs/>). Ny `yearSwing` (Δ siden 12 mdr, via closingBalanceOnOrBefore(addMonthsYmd(today,-12))) i getNetWorth + NetWorth-type + 'Dette år'-swing på forside + /finans (.swings wrapper nu). Operatør-kort: fuldt navn "Gustav Bondebjerg Smith". (2) PRODUKTIVITETS-MODUL kodet + verificeret, IKKE committet (afventer commit-ja): erstatter de FAKE fokus+streak på operatør-kortet med ÆGTE tal. Fokus = øverste uge-mål (listGoals, scope 'week'; fallback "Sæt et ugefokus"-link). Streak -> Gustavs idé om en produktivitetsprocent, men DELT i to rene tal så glemt logning ikke vælter headline (han logger kun ~40% af dagen endnu): PRODUKTIVITET = produktiv tid / logget ikke-søvn tid (kvaliteten af det loggede); DÆKNING = logget ikke-søvn / vågne timer (30*24 - søvn). Ganget = hans oprindelige "andel af vågen tid produktiv". Produktiv = studie/arbejde/projekt/socialt/krop; fritid/transport/andet+huller tæller ikke; søvn helt udenfor. Migration `0016_productivity_snapshots.sql` (KØRT i prod via run-migration). `lib/productivity.ts` (server-only): computeProductivity GENBRUGER getBalance(30) (Sonnet-kategorisering + 1t cache); snapshotProductivity (upsert pr. dato, idempotent); getLatestProductivity (kortet læser kun gemt tal); runProductivityCron. Cron `app/api/cron/productivity/route.ts` (validateCronRequest + withCronLock 'productivity') schedulet dagligt `0 3 * * *` i vercel.json - tungt Sonnet-kald precomputes om natten, så forsiden er lynhurtig og uden per-load-pris/jitter. VERIFICERET end-to-end mod prod via lokal dev (env -u ANTHROPIC_API_KEY npm run dev + scripts/test-productivity-cron.mjs): HTTP 200, produktivitet 88,3% / dækning 39,4% / 89 events; read-back af gemt række bekræftede mapping (produktiv 189,8t / logget 215,1t / søvn 173,8t / vågne 546,3t; 88,3=189,8/215,1, 39,4=215,1/546,3). tsc+lint rene. FALDGRUBE bekræftet igen: getBalance bruger unstable_cache (kun Next-runtime), så testet via cron/dev, ikke bart script. NÆSTE: commit produktivitets-modulet efter Gustavs ja.
- NYT - NETTOFORMUE-KURVE (net_worth_snapshots) KODET + VERIFICERET 2026-06-04 (committet 3b7d84a + pushet + deployet). Den sidste udestående brik - færdiggør Modul 4 og dermed HELE byggeplanen. Tabellen fandtes allerede (migration 0013), så INGEN ny migration. `lib/finance.ts`: `snapshotNetWorth(date?)` (upsert ét dagligt snapshot: checking = closingBalanceOnOrBefore(date), net_worth = checking + manuel netto; idempotent pr. snapshot_date), `backfillNetWorthSnapshots()` (henter ALLE posteringer ÉN gang pagineret, grupperer pr. dato, regner dagens luk-saldo via ny DELT helper `dayClosingBalance` (udtrukket af closingBalanceOnOrBefore så begge bruger samme saldo-kæde-logik), bulk-upserter ét snapshot pr. transaktionsdag - saldoen ændrer sig kun på posteringsdage; manuelle balancer antaget = nuværende for historikken), `getNetWorthHistory(days=180)`. Dagligt snapshot kører nu i finans-cronen (`runFinanceClassifyBatch` -> snapshotNetWorth, best-effort, nyt `snapshot`-felt i FinanceClassifyResult); backfill kører efter hver bank-import (`importBankAction`, best-effort - udvider kurven med nye dage). UI: ny `components/NetWorthCurve.tsx` (ren inline-SVG, INGEN charting-dependency; compact=sparkline + fuld=med dato/beløbsakse; preserveAspectRatio='none' + non-scaling-stroke; bevidst INGEN <circle>-prik da den ville strækkes til en ellipse af 'none') i /finans Nettoformue-kort (180 dage) + compact sparkline på forsidens finans-kort (90 dage). NetWorthPoint-type i finance-shared (re-eksporteret). CSS: .nw-curve/.nwc-svg/.nwc-line/.nwc-area/.nwc-axis. Nyt script `scripts/finance-snapshot.ts` (backfill + dagens snapshot, idempotent, genkørbar). VERIFICERET mod prod: backfill skrev 510 daglige snapshots (2024-05-01=16014 kr -> 2026-06-04=1224 kr, seneste matcher getNetWorth), tsc+lint rene, /finans + forsiden kompilerer rent (HTTP 307, ingen compile-fejl i dev-log). NB: visuel render bag login ikke set af agenten (auth-gated) - typer+data+ren komponent verificeret i stedet. NÆSTE: commit efter Gustavs ja -> byggeplanen er 100% færdig.
- NYT - Modul 5 UGENTLIG REVIEW LIVE 2026-06-04 (committet d367c75 + pushet + deployet; cron-route `/api/cron/review` verificeret live, denne uges review allerede genereret + leveret på Telegram). Migration `0015_weekly_reviews.sql`: tabel `weekly_reviews` (week_start date UNIQUE = mandag i ISO-ugen, week_end, report markdown, data jsonb-snapshot, delivered_telegram, generated_at) + RLS deny-all. KØRT i prod via run-migration. `lib/weekly-review.ts` (server-only): `generateWeeklyReview({store,deliver,force})` samler ugens data (getBalance(7) for tid - BEST-EFFORT i try/catch så én fejlende kilde ikke vælter reviewet; listTasks åbne + direkte query for fuldførte-i-ugen; listGoals; getWeeklyFinanceSummary), bygger persona-prompt (wins / hvad smuttede / åbne sløjfer / top 3 næste uge, max ~250 ord, ingen em-dash), kalder Claude Sonnet (`claude-sonnet-4-6`), gemmer idempotent pr. uge (upsert onConflict week_start), indekserer i second brain (storeChunk source_type='weekly_review', source_id = RÆKKENS UUID - memory_chunks.source_id er en uuid-kolonne, IKKE datoen; det var en bug fanget i test), kan levere på Telegram (trunkeret 3900 tegn). Idempotent: findes ugen + er leveret, springer cron-retries over (ingen dobbelt Sonnet/Telegram). `getLatestReview`/`listReviews`. Ny finans-helper `getWeeklyFinanceSummary()` i `lib/finance.ts` (nettoformue + ugens konto-Δ via closingBalanceOnOrBefore(today-7) + denne måneds synder). Cron `app/api/cron/review/route.ts` (validateCronRequest + withCronLock 'weekly-review' + `runWeeklyReviewCron`) schedulet søndag 18:00 UTC via vercel.json (`0 18 * * 0`; INGEN pg_cron - ugentlig rammer Vercel Hobby fint, pr. planen). UI: `app/review/page.tsx` (server: seneste review + arkiv som <details> + stats-strip) + `components/ReviewReport.tsx` (lille markdown-renderer for ## overskrifter, - bullets, 1. ordered, **fed** - INGEN dependency, inputformatet styres af prompten) + `components/ReviewGenerate.tsx` (klient: 'Generér nu'-knap, force=true, leverer IKKE Telegram) + `app/review/actions.ts` (generateReviewAction, auth-gated). Nav: 'Review'-link mellem Balance og Captures i `app/layout.tsx`. CSS i globals.css (review-report/rr-h/review-stats/review-archive m.m. + `.review-gen-btn` på upl-btn-reglen). Scripts: `scripts/weekly-review.ts` (dry-run generering, --store/--deliver) + `scripts/test-review-cron.mjs` (HTTP smoke-test m. Bearer). VERIFICERET end-to-end mod prod: dry-run skrev review forankret i ægte tal (nettoformue 1224, ugens Δ -1498, synder takeaway 675/spil 310, åben opgave 'fly tjek-in'); fuld cron-sti via lokal dev gav HTTP 200 (created+delivered), DB-tjek bekræftede tid hentet i runtime (trackedHours 98t) + memory_chunks=1 (indeksering virker efter uuid-fix) + 2 test-reviews leveret på Gustavs Telegram. tsc+lint rene. FALDGRUBE: getBalance bruger unstable_cache der KRÆVER Next-runtime - et bart tsx-script kan ikke kalde den (Invariant: incrementalCache missing), derfor best-effort + cron/dev for at teste tid. Plan: `~/.claude/plans/jeg-vil-gerne-have-stateless-pascal.md` (Modul 5). NÆSTE: commit efter Gustavs ja -> Modul 1-5 komplet; eneste rest er net_worth_snapshots (historisk nettoformue-kurve).
- NYT - Modul 4 PERSONLIG FINANS KODET + VERIFICERET 2026-06-04 (committet 888c554 + a99b99e + 97317cb, alt pushet + deployet). Migration `0013_finance.sql`: 6 tabeller (transactions, transaction_lines, storebox_receipts, net_worth_snapshots, manual_balances, finance_imports), alle RLS deny-all. KØRT i prod via `run-migration.mjs`. DATAKILDER: bank-CSV (ISO-8859-1, semikolon, komma-decimal, dato DD-MM-YYYY, INGEN header; kolonner 0-indekseret: 3=dato, 4=tekst, 5=beløb, 6=saldo, 7=modpart, 8=detalje med Forretning/By/Kortnr) + Storebox-eksport (en MAPPE: receipts-*.json = array af kvitteringer m. varelinjer, + user/cards/attachments-pdf'er). RENE parsere (klient-sikre, INGEN server-only, så de kan importeres af test-scripts + klient): `lib/finance-shared.ts` (CATEGORIES, SIN_TAGS, typer, `cleanText` der fjerner kontrol/NUL-bytes som Postgres' text ikke kan gemme), `lib/finance-csv.ts` (`parseBankCsv` - dekodning sker i kalderen), `lib/finance-storebox.ts` (`parseStoreboxReceipts`), `lib/finance-reconcile.ts` (`reconcile`: |beløb| eksakt + dato ±3 dage + forretning fuzzy, 2-runde, 1:1). `lib/finance.ts` (server-only): `importBankCsv` (idempotent via `import_hash`=sha256(dato|tekst|beløb|saldo|ref)), `importStoreboxReceipts` (idempotent via `receipt_id`) + `reconcileUnmatched` (KRITISK: paginerer ALLE rækker i sider à 1000 - PostgREST capper ellers default på 1000 og reconcile matcher næsten intet; det var en reel bug der gav 1 match i stedet for 132), `getNetWorth` (`closingBalanceOnOrBefore` rekonstruerer dagens luk-saldo fra SALDO-KÆDEN: luk-saldoen er den der ikke er nogen andens "før-saldo"=balance-amount, fordi same-day-rækkefølge ikke gemmes ved bulk-insert), `getSinSummary` (forbrug pr. sin_tag denne måned, transaktions- + varelinje-niveau uden dobbelttælling), `listTransactions`/`getTransactionLines`, manual balances CRUD, `setTransactionCategory`. `lib/finance-classify.ts` (server-only): Haiku batch-klassificering. KRITISK: org rate-limit = 50 Haiku-kald/min -> global `pace()` serialiserer kald-START til ~1,35s (44/min) + 429-retry m. backoff; trunkeret JSON -> kør med `batchSize 10` + per-batch try/catch så ét dårligt svar ikke dræber kørslen. Matchede transaktioner sættes til category='dagligvarer' (sin på LINJERNE); umatchede får AI category+sin. KØRSEL af alle finans-scripts: `node --conditions=react-server --import tsx scripts/finance-X.ts` (react-server gør `server-only` til no-op uden for Next; `load-env` overskriver shellens tomme ANTHROPIC_API_KEY; prefix `cd .../gustav-os &&` da cwd nulstiller). Scripts: `finance-dryrun.ts` (parser+match-BEVIS UDEN DB), `finance-import.ts` (idempotent bulk-load), `finance-classify.ts` (klassificering), `finance-verify.ts` (verifikation). RIGTIGE filer ligger UDEN FOR repoet i `~/Developer/gustav-os-private/finans/` (gitignored sikkerhedsnet i repoet: `/private/`, `/finans-data/`, `*.csv`). Gustavs bank-eksport går KUN tilbage til 2024-05-01 (Storebox til 2016) -> kun 145 kvitteringer i bank-perioden, 132 matchet (91%); de ~934 pre-2024 IGNORERES bevidst (ingen bank-modpart) pr. Gustavs instruks. UI: `app/finans/page.tsx` (server: nettoformue-kort + synde-kort + balancer + upload + posteringsliste) + `components/FinanceManualBalances.tsx` (opsparing/investering/gæld, router.refresh) + `FinanceUpload.tsx` (månedlig bank-CSV + receipts-*.json, samme idempotente sti som bulk-load) + `FinanceTransactions.tsx` (kategori-`<select>` der retter AI-fejl + udfoldelige varelinjer via `getLinesAction`). `app/finans/actions.ts` (importBank/importStorebox/reconcile/manual balances/setCategory/getLines - auth-gated + revalidate) + `state.ts`. Forsidens "03 Finans"-kort wirer nu RIGTIGE tal (nettoformue + dagligt/månedligt sving + top-5 synder, linker til /finans) - placeholder (SPARK/SINS) fjernet. Ny dep: `tsx` (installeret `--no-save`, IKKE i package.json - kun til at køre .ts-scripts lokalt). tsc + lint + `next build` RENE (alle ruter inkl /finans). SPØRGE-LOOP (web) BYGGET 2026-06-04 (ucommittet): klassificeringen anvender nu lærte regler FØRST (`loadFinanceRules` fra memory_facts scope 'finance', key=`merchantToken` udledt af bank-teksten), så AI med konfidens (lav -> `status='needs_review'`). Review-kø på /finans (`listReviewQueue` = needs_review + 'andet' + ukategoriseret, 40 ad gangen, .or-filter) som kort "Til gennemsyn" der genbruger `FinanceTransactions`' kategori-`<select>`. Gustavs rettelse (`setCategoryAction`) gemmer en regel (`saveFinanceRule` -> `saveMemory`) + anvender den RETROAKTIVT på ALLE usikre posteringer med samme forretnings-token (`applyLearnedCategory`) - ret én Netto, alle usikre Netto rettes med (best-effort, vælter ikke kategori-sætningen ved fejl). Læringen verificeret end-to-end mod prod (test-og-revert script, slettet igen). Nyt read-only inspektions-script `scripts/finance-review.ts`. MÅNEDLIG IMPORT-FLOW (selvbetjent) BYGGET 2026-06-04 (ucommittet): upload bank-CSV + Storebox receipts-*.json på /finans (idempotent - Storebox sendes som fuld historik hver måned, dedup på receipt_id; bank dedup på import_hash), import+matchning STRAKS. Ny cron `/api/cron/finance-classify/route.ts` (validateCronRequest + withCronLock + `runFinanceClassifyBatch` = BUNDET portion: markMatched (gratis) + maxReceipts kvitteringer + maxTransactions posteringer, holder sig under 60s + 50/min rate-limit) dræner kategoriseringskøen i baggrunden. Schedulet hvert kvarter via pg_cron migration `0014_finance_classify_cron.sql` (genbruger Vault-secret + http_get-mønster fra 0007; KØRT i prod via run-migration, job `gustav-os-finance-classify` = jobid 4) + daglig vercel.json-fallback. Import-actions sender kort Telegram-OPSUMMERING ("💳 Bank importeret: X nye..." / "🧾 Storebox: Y matchet..."), best-effort. Per-postering Telegram-ASK blev BEVIDST fravalgt (Gustav + agent: web-review + opsummering slår pings pr. usikker postering). Verificeret: endpoint HTTP 200 (auth+lock+batch), bunden klassificering mod rigtige data (test-og-revert), tsc+lint+build rene. Nyt script `scripts/test-finance-cron.mjs`. UDESTÅR (eneste rest i Modul 4): `net_worth_snapshots` dagligt for en historisk nettoformue-kurve (kortet viser kun nuværende + sving). COMMITTET (97317cb) + pushet 2026-06-04; pg_cron -> pg_net -> prod verificeret end-to-end (HTTP 200). Modul 5 (ugentlig review) nu bygget - se øverste bullet. Plan: `~/.claude/plans/jeg-vil-gerne-have-stateless-pascal.md` (Modul 4).
- NYT - AGENT-SELVKØRSEL opsat 2026-06-04: Gustav lavede et Supabase personal access token og lagde det i `.env.local` som `SUPABASE_ACCESS_TOKEN`. Det giver agenten direkte adgang uden manuelle skridt. Tre nye scripts: `scripts/run-migration.mjs <nr>` kører en migration via Supabase Management API (`POST /v1/projects/<ref>/database/query`) - DDL virker, modsat service_role/PostgREST; `scripts/test-telegram.mjs "besked" [local|prod]` POSTer en SYNTETISK Telegram-update til webhooken (med X-Telegram-Bot-Api-Secret-Token + chat_id) så bot-flowet kan testes UDEN telefon (bottens svar går dog til Gustavs rigtige chat som side-effekt); `scripts/verify-modules.mjs` læser tasks/goals via service_role til verifikation. MCP-ruten (`claude mcp add @supabase/mcp-server-supabase`, pakke verificeret v0.8.1) viste sig UNØDVENDIG - og `claude`-CLI'en var alligevel ikke i Gustavs PATH (ikke npm-global, ikke i ~/.local/bin etc.). Vercel: ingen npm-MCP, men `git push` auto-deployer allerede, så intet behov. Via denne vej kørte agenten 0011 + 0012 i prod + verificerede Modul 2/3 funktionelt. VIGTIGT for fremtidige agenter: kør migrationer med `node scripts/run-migration.mjs <nr>` og test Telegram med `node scripts/test-telegram.mjs "..."` - ingen grund til SQL Editor / telefon længere. Dev-server skal startes med `env -u ANTHROPIC_API_KEY npm run dev` (Claude Code sætter en tom key der ellers brækker AI-kald lokalt).
- NYT - Modul 3 MÅL KODET 2026-06-04 (committet 32b0337 + LIVE; migration 0012 kørt i prod). Migration `0012_goals.sql`: ny tabel `goals` (id, scope text check ('week'|'month'), title, done boolean, sort int, created_at, updated_at) + index `idx_goals_scope` + RLS deny-all. SKAL KØRES i Supabase SQL Editor (projekt dxowfjyigfrhyaixyonj). Bevidst SIMPELT pr. den approve'de plan: ét mål = én boolean (klar/ikke klar), bliver stående til Gustav fjerner det, ingen auto-nulstilling, ingen AI. Claude Designs rigere "aktivt fokus + delmål"-hero er UDSKUDT (kræver eget datamodel - flagget til senere, ikke i plan-scope for Modul 3). `lib/goals-shared.ts` (klient-sikkert: SCOPES, SCOPE_LABEL, Goal-type, isScope) + `lib/goals.ts` (server-only, re-eksporterer shared): listGoals / addGoal / toggleGoal / removeGoal. UI: `app/maal/page.tsx` (server -> listGoals) + `components/GoalsBoard.tsx` (klient: to grupper Denne uge/Denne måned, hver med fh-task-checkbokse + tilføj-felt + fjern-x, optimistiske mutationer via server actions) + `app/maal/actions.ts` (addGoalAction/toggleGoalAction/removeGoalAction - auth-gated + revalidate /maal + /) + `app/maal/state.ts`. Forsidens "06 Mål"-kort wirer nu RIGTIGE mål (to grupper, done/total, read-only fh-task-rækker der linker til /maal) - placeholder-progressbarer fjernet. Ny CSS: `.maal-grid`, `.goal-list`, `.goal-text`, `button.fh-check`-reset, `div.fh-task`/`a.fh-task` cursor-skel. tsc + lint + produktions-build rene. NÆSTE: kør 0012, test på :3000 (tilføj uge-/måneds-mål, afkryds, fjern, tjek forsidekort), commit efter Gustavs ja.
- NYT - Modul 2 OPGAVE-BOARD + auto-fangst KODET 2026-06-04 (committet 32b0337 + LIVE; migration 0011 kørt i prod). Migration `0011_tasks_board.sql` udvider `tasks` med `urgency` ('today'|'week'|'month'|'someday'), `key` (vigtig bool), `priority_score` (0-100, AI-tildelt), `completed_at`, `tags` (jsonb), index `idx_tasks_board` + backfill (åbne opgaver -> 'someday'). SKAL KØRES i Supabase SQL Editor (projekt dxowfjyigfrhyaixyonj) FØR boardet virker - der er ingen migration-runner/CLI, og service_role/PostgREST kan ikke køre DDL. Nyt klient-sikkert lag `lib/tasks-shared.ts` (URGENCIES, URGENCY_LABEL, Task-type, isUrgency - INGEN server-import, så `components/TaskBoard.tsx` (klient) kan bruge det uden at trække `server-only` ind). `lib/tasks.ts` (`server-only`, re-eksporterer shared): listTasks / createTask / completeTask / reopenTask / moveTask / deleteTask + `classifyTask` (Haiku udleder ren titel + urgency + key + priority_score + due_date) + `createTaskFromCapture`. AUTO-FANGST (Gustavs kernekrav): en capture klassificeret som type=opgave bliver til en prioriteret opgave - hægtet på i BÅDE `lib/capture.ts` (saveCapture returnerer nu `task`; web-capture-stien) OG `lib/telegram-webhook.ts` (handleCapture: ny opgave-gren EFTER aftale-grenen, bekræfter "✅ Opgave oprettet: ... Prioritet: I dag · vigtig"). INGEN inline-knapper i Telegram (sendTelegramMessage understøtter ikke reply_markup + ingen callback_query-handler; bevidst v1-fravalg, justering sker på /opgaver). UI: `app/opgaver/page.tsx` (server -> listTasks) + `components/TaskBoard.tsx` (klient, Claude Design-prototypens Opgaver-fane: vhead + område-filter-chips + add-row med AI-prioritering + 4 hastigheds-bunker; optimistiske mutationer via server actions; flyt via `<select>`, slet via x-knap). `app/opgaver/actions.ts` (addTaskAction/toggleTaskAction/moveTaskAction/deleteTaskAction - hver auth-gater + revalidaterer /opgaver + /) + `app/opgaver/state.ts` (klient-returtyper). Forsidens "04 Dagens opgaver"-kort wirer nu RIGTIGE data (åbne opgaver med urgency=today ELLER key=true, top 3, hele rækken linker til /opgaver) - placeholder fjernet. Ny CSS i globals.css (`button.check`, `.t-side/.t-move/.t-del`, `a.task` uden underline, `.board-foot`). Ny helper `isPastDayCph` i `lib/format.ts` (forsinket-markering på deadlines). tsc + lint + produktions-build rene (next build: alle ruter inkl /opgaver). NÆSTE: (1) kør 0011 i Supabase, (2) test på :3000 - tilføj i add-row (AI-prioritet), Telegram "jeg skal nå at X" -> "✅ Opgave oprettet", toggle/flyt/slet, tjek forsidekort, (3) commit efter Gustavs ja. Plan: `~/.claude/plans/jeg-vil-gerne-have-stateless-pascal.md` (Modul 2).
- NYT - Modul 1 REDESIGNET efter Claude Design-handoff (committet 1e77fc2 + pushet til main 2026-06-04, Vercel auto-deployede). Gustav lavede en high-fidelity "mørk cockpit" i Claude Design (handoff-bundle udpakket i `/tmp/design_extract/personalos/`, primær fil `project/gustav-os Dashboard.html` + `os-data.js` + `os-views.js` + grundig `design_handoff_.../README.md`). Jeg portede "I dag"-fanen + fundamentet. Implementeret: design-tokens (bg #090a12, accent #694aaf, ink-skala, pos/neg/warn, spacing/radius/shadow) + HELE prototypens komponent-CSS i `app/globals.css`; fonts IBM Plex Sans/Mono + Newsreader via `next/font/google`; nye komponenter `components/Card.tsx` (glas-kort), `components/Clock.tsx` (live topbar-ur), `components/SessionCard.tsx` (klient: levende hilsen/ur + "dagens ene ting" i localStorage + capture via eksisterende `createCaptureAction`); `app/layout.tsx` = ny topbar (brand m. pulse-dot, 7 faner som accent-piller, live-ur, log ud); `app/page.tsx` = "I dag" bento (struct D, 6 kort). Tidslinjen "05 I dag" er RIGTIG kalenderdata (`getEvents`) med past/now/next; Operatør/Finans/Opgaver/Mål er placeholder-data tro mod prototypen indtil modul 2-4. Nye stub-ruter `app/opgaver|maal|finans/page.tsx` (holder fanerne uden 404). Avatar `public/gustav.jpg` (placeholder fra bundle). Ny dep `lucide-react`. NU DARK-ONLY (`.dark` på `<html>` + `@custom-variant`); de endnu-ikke-redesignede sider (captures/balance/ask/actions/login) renderer dark via egne `dark:`-varianter. Fjernet fra forsiden: "Venter på dig" (veto) + "Seneste captures" (captures er egen fane). Checks RENE: tsc exit 0, lint exit 0, build exit 0. Smoke mod kørende dev (:3000): /login 200 (Plex + dark), / 307, /opgaver 307. UDESTÅR (afventer Gustav): (1) backend-veto. UI-vetokortet er væk, men BACKEND-vetoen (aftale -> forslag -> 10 min veto-vindue -> cron eksekverer; `lib/telegram-webhook.ts`, `lib/actions-runner.ts`, `actions`-tabel, cron) er IKKE rørt - afklar om kalender-skriv skal ske straks/automatisk eller helt manuelt. (2) De 6 andre faner (Opgaver/Mål/Finans/Balance/Captures/Ask, i `os-views.js`) er IKKE portet endnu - bygges i deres moduler. NB: forsidens design afviger bevidst fra det første Modul 1-udkast (indigo glas) - Claude Design vandt, jf. Gustavs besked. Plan: `~/.claude/plans/jeg-vil-gerne-have-stateless-pascal.md`.
- NYT - 10-minutters veto FJERNET (2026-06-04, Gustavs valg "skriv straks, ingen veto"; løser UDESTÅR (1) i bulletten ovenfor). Kalender-handlinger fra Telegram udføres nu STRAKS: aftale -> `insertEvent`, slet -> `deleteEvent`, ret -> `updateEvent` (ret var allerede straks). Ændret KUN i `lib/telegram-webhook.ts`: `handleCapture` (aftale- + slet-gren) og `handleAgentCreate` skriver direkte + bekræfter ("✅ Lagt i kalenderen" / "🗑️ Slettet fra kalenderen") + logger i `audit_log` (applied/failed), i stedet for at oprette en `proposed` action + veto-besked. `formatProposal`/`formatDeletion` erstattet af `formatInsertDone`/`formatDeletionDone`; `deleteEvent` importeret. Ingen nye `actions`-rækker oprettes mere. IKKE rørt: `lib/actions-runner.ts` + `/api/cron/actions` + pg_cron (finder bare 0 due actions nu, harmløst); `handleVeto`/`findVetoTarget`/`isVetoMessage`/`VETO_WORDS` bevaret som no-op safety (`nej`/`stop` svarer "Ingen forslag at vetoe"). `actions`-tabel + `VETO_MINUTES`-env nu ubrugt men urørt (ingen migration). AGENTS.md persona-regel opdateret (var "auto-handlinger har altid veto"). tsc/lint/build rene. Committet 4b95d98 + pushet til main 2026-06-04 (Vercel auto-deployede); Gustav bekræftede "Det funger".
- Milestone 0: Next.js 15 + TS + Tailwind, git, secrets gitignored, CLAUDE.md, memory.
- Fase 1: Supabase forbundet, schema kørt (6 tabeller + pgvector + RLS), round-trip verificeret.
- Nøgler: Anthropic + OpenAI + Telegram + Google service account valideret. Telegram-bot LÅST til Gustav (`TELEGRAM_CHAT_ID` sat).
- Fase 2 (tekst + voice): long-polling capture. Voice -> Whisper (whisper-1, dansk). Klassificering med Claude Haiku. Område: personlig/studie/arbejde. Type: opgave/note/ide/aftale. Capture er helligt: råindhold gemmes FØR klassificering.
- Fase 3 (kalender + balance): Google service account (kun læseadgang), primær kalender delt med robotten. `scripts/calendar.mjs` henter events via JWT-auth (`google-auth-library`). `scripts/balance.mjs` regner præcise timer pr. kategori i kode, Claude Sonnet 4.6 kategoriserer + skriver rapporten i personaen. Levering: terminal eller `--telegram`.
- Fase 4 (auto-handlinger + Telegram-veto): Google service account opgraderet til "Make changes to events". Ny `actions`-tabel (state machine: proposed/executed/vetoed/failed). `scripts/calendar-write.mjs` indsætter + sletter events (scope `calendar.events`). `scripts/propose.mjs` lader Haiku udlede {summary, start, end, location?} fra capture-tekst, eller returnere null hvis tiden er vag. `scripts/telegram-poll.mjs` udvidet: ved type=aftale sendes forslag, ved enkelt-veto-ord markeres nyeste proposed action som vetoed. `scripts/watch-actions.mjs` udfører forslag efter veto-vindue, opdaterer status, logger i `audit_log` med statusserne `applied`/`vetoed`/`failed`. Default veto-vindue 10 min, kan sættes lavere under test via `VETO_MINUTES` env.
- Fase 5 (memory/ask): Ny migration `0003_memory_search.sql` med `search_memory(query_embedding, match_count, filter_area)` RPC. `scripts/embed.mjs` (delt modul: `embedText` + idempotent `storeChunk`, OpenAI text-embedding-3-small). `scripts/embed-captures.mjs` backfilder eksisterende raw_captures. `scripts/ask.mjs` embedder spørgsmål, kalder RPC, sender top-8 chunks + spørgsmål til Claude Sonnet 4.6 med Gustav OS-personaen, returnerer svar med citerede kilder. `telegram-poll.mjs` udvidet: `/ask <spørgsmål>` på Telegram + auto-embed efter hver capture (best-effort, blokerer ikke capture-gemning hvis OpenAI fejler). Idempotens via UNIQUE (source_type, source_id)-tjek i `storeChunk` så backfill kan køres flere gange.
- Fase 6 (dashboard): Next.js 16 (Turbopack) på localhost:3000. Top-nav layout (`app/layout.tsx`) med Captures/Actions/Ask. `/captures` viser raw_captures (område-badge, type, kilde, timestamp, content). `/actions` viser actions med status-badge (proposed=amber, executed=grøn, vetoed=grå, failed=rød) + Google Calendar-link når executed. `/ask` er client component med `useActionState`, kalder server action `askAction` der bruger `lib/ask.ts` (TS-port af scripts/ask.mjs). `lib/supabase.ts` har service_role-klient (RLS bypass; OK fordi localhost-only). Ingen auth - tilføjes i Fase 7 ved deploy. Server-action-faldgrube: `'use server'`-filer må kun eksportere async funktioner; types/constants ligger i separat fil (`app/ask/state.ts`).
- Fase 7 led 1 (auth): Supabase Auth med magic link og ÉN whitelistet email (`ALLOWED_EMAIL` i `.env.local`). `proxy.ts` (Next.js 16 file convention; afløser `middleware.ts` fra v15) gater alle non-public routes - redirecter unauth til `/login`, sender authed brugere væk fra `/login` til `/captures`. `lib/supabase-server.ts` er anon-klient med cookies (server components, server actions, route handlers). `lib/supabase-browser.ts` er anon-klient til client components. `lib/supabase.ts` er nu mærket `'server-only'` så service_role-importen knækker buildet hvis nogen ved en fejl trækker den ind i et client component. `/login` er form med magic link, fremmede emails får "Tjek din inbox" uden at der faktisk sendes noget (silent rejection). `/auth/callback` exchanger code, tjekker whitelist + signer ud hvis fremmed, redirecter til `/captures`. `/auth/signout` er POST-only (GET = 405) og redirecter 303 til `/login`. `askAction` i `/ask` har sin egen auth-guard fordi Next.js docs advarer om at proxy-matcher kan smutte server actions. Top-nav viser nu logget-ind email + "Log ud"-knap. Layoutet skjuler nav på `/login` og `/auth/*`. Smoke-test: alle gated routes returnerer 307 til `/login` uden session, `/login` returnerer 200, `POST /auth/signout` returnerer 303. `npx tsc --noEmit` ren.
- Fase 7 led 2 (webhook): `/api/telegram` route oprettet. Secret-header tjek, chat_id whitelist, idempotens via `telegram_updates`, tekst/voice capture, `/ask`, veto og kalenderforslag virker gennem server-side TS-flowet i `lib/telegram-webhook.ts`. Ny migration `0004_telegram_webhook.sql`. Ny admin-CLI `scripts/telegram-webhook.mjs` (`get`, `set <url>`, `delete`). `lib/memory.ts` udtrukket så dashboard-ask og webhook bruger samme embedding-helper. `lib/format.ts` + `lib/ask-types.ts` holder client-sikre helpers/typer væk fra server-only kæden. `proxy.ts` matcher ikke længere `/api/*`.
- Fase 7 led 3 (cron): `/api/cron/actions` kører due actions efter veto-vindue. `/api/cron/patterns` analyserer 14 dage og sender kun mønster-flag hvis der er et konkret signal. (`/api/cron/morning`+`/api/cron/evening` blev senere afskaffet 2026-06-03.) Ny migration `0005_cron.sql` med `cron_locks`, `claim_cron_lock`, `release_cron_lock` og `actions.processing_started_at`. Ny `vercel.json` med daglige schedules. `scripts/watch-actions.mjs` claimer nu actions som `executing`, så lokal watcher og cron ikke bør dobbelt-udføre samme proposed action. Actions-dashboard viser også `executing`.
- Ekstern cron-forsøg (2026-05-31): `.github/workflows/cron-actions.yml` blev oprettet og pushet. Workflowet kalder `GET "$PROD_URL/api/cron/actions"` med `Authorization: Bearer $CRON_SECRET` og bruger GitHub secrets `PROD_URL` + `CRON_SECRET`. Manuel dispatch returnerede `{"ok":true,...}` og beviste at endpoint, auth og secrets virker. Vercel daily cron i `vercel.json` blev bevidst bevaret som fallback. GitHub `schedule`-events kørte dog ikke efter flere tests (`*/10`, `3/10`, og eksplicit `3,13,23,33,43,53 * * * *`) plus enable/disable og repo Actions settings. `schedule-smoke.yml` blev tilføjet for at isolere problemet; den virker manuelt, men fik heller ikke schedule-runs. Konklusion: brug `cron-job.org` eller tilsvarende ekstern scheduler som primær løsning, ikke GitHub Actions schedule.
- Fase 7 led 4 (deploy): Vercel production deploy live på `https://gustav-os.vercel.app`. Telegram webhook live på `https://gustav-os.vercel.app/api/telegram`. Supabase Auth prod URL/redirect sat af Gustav. Migrations kørt i korrekt Supabase-projekt (`dxowfjyigfrhyaixyonj.supabase.co`). Vercel Cron er aktiv via `vercel.json`, men Hobby-planen kører kun daglige schedules.
- Dashboard v2 led 1 (root-overblik): `app/page.tsx` er nu et server component med tre sektioner. "I dag" henter calendar-events via `getEvents(startOfTodayCph, endOfTodayCph)` (CPH-tid, ikke server-UTC) og filtrerer afsluttede events. "Venter på dig" lister ALLE proposed actions (også dem hvor veto-deadline er passeret, fordi Hobby-cron kun kører dagligt); efter deadline står "venter på cron" i stedet for "veto X min". "Seneste captures" viser 5 nyeste med område-badge og line-clamp-2. Kalender-fejl wrappes i try/catch. `lib/format.ts` har nu Intl.DateTimeFormat-baserede helpers (`fmtDate`, `fmtRange`, `fmtDay`, `isTodayCph`, `startOfTodayCph`, `endOfTodayCph`) der altid viser Europe/Copenhagen. Top-nav i `app/layout.tsx` har nu "I dag" som første link. `tsc --noEmit` ren. `next build` ren (7 min på cold cache).
- Slet-flow (calendar_delete): nyt action-type. `lib/telegram-webhook.ts` detekterer slet-intent via regex (`slet|fjern|aflys|aflyse|aflyst|cancel|drop`) og kalder `proposeCalendarDelete(content)` der henter events 14 dage frem og lader Haiku finde bedste match. Match → action med type=`calendar_delete` og payload `{event_id, summary, start, end, location?}`. Veto fungerer som før (samme `findVetoTarget`, men besked tilpasset "slettes ikke fra kalenderen"). `lib/actions-runner.ts` håndterer nu både insert og delete: `deleteEvent(event_id)` fra `lib/calendar.ts`, 410 Gone tæller som success. `/actions` viser "Slettet fra Google Calendar" for executed delete-actions (intet htmlLink fordi event er væk). `formatProposal` + `formatDeletion` bruger nu `fmtDay`/`fmtRange` så Telegram-beskeder viser CPH-tid (insert-flowet havde også UTC-bug). `audit_log.action` afspejler nu faktisk action-type, ikke hardcoded "calendar_insert".
- Adaptiv morgenbrief (AFSKAFFET 2026-06-03; var sidste byggesten per original plan): `lib/proactive.ts` -> `buildMorningBriefing` klassificerer dagen som `early` (første event før 08:00 CPH), `normal` (08-12), `late` (efter 12) eller `empty` (ingen events). Klassen passeres til Claude som eksplicit `adaptHint` i system-prompten, så indholdet skifter tone og længde med dagsformen. Hobby tillader stadig kun daglig cron, så tidspunktet (05:00 UTC) er uændret; indholds-adaptionen er den maksimale adaption inden for Hobby-grænserne.
- Dashboard v2 led 4 (web-capture): ny `lib/capture.ts` med `classify`, `saveCapture`, `VALID_AREAS`, `Classification` og `CaptureSource`. `saveCapture({ content, source })` gemmer rå capture, klassificerer (best-effort), embedder (best-effort). `lib/telegram-webhook.ts` importerer nu de samme symbols i stedet for at have sin egen `classify`/`Classification`. `app/captures/page.tsx` har en ny `<NewCaptureForm />` øverst (client component med `useActionState`). Server action `createCaptureAction` i `app/captures/actions.ts` tjekker auth (samme guard som `askAction`), validerer længde (max 5000 tegn), kalder `saveCapture` med `source: 'dashboard'`, kalder `revalidatePath('/captures')` + `revalidatePath('/')` så listen + root-overblik opdateres. Source-filter på `/captures` har nu `web`-chip; mapping fra UI-værdi til DB-værdi via `SOURCE_DB_MAP` (`text→telegram_text`, `voice→telegram_voice`, `web→dashboard`). Source-label på cards: `text` / `voice` / `web` via `labelSource()`-helper både i `/captures` og `/` (root-overblik).
- Dashboard v2 led 3 (filtre + søgning): `/captures` har nu filter-bar over listen. Query-params: `q` (tekstsøgning, ILIKE med `%` og `_` escaped), `area` (personlig/studie/arbejde, valideret mod whitelist), `type` (opgave/note/ide/aftale, filtreret via `classification->>type` på JSONB), `source` (text/voice, mappet til `telegram_text`/`telegram_voice`). Filtre kombineres med AND. Chips er `<Link>`-elementer der toggler det enkelte filter (klik aktivt chip = fjern). `buildHref(current, override)` bygger næste URL og bevarer ikke-overridede filtre. Søge-form er GET med hidden inputs for aktive chips, så søgning ikke nulstiller dem. "Nulstil"-link når mindst ét filter er sat. Resultat-tæller viser "(filtreret)" når relevant. Eksisterende kort-layout uændret.
- Tids-tracking via Telegram: ny `pending_activity`-tabel (migration `0006_pending_activity.sql`, én aktiv række per chat_id via UNIQUE-constraint + upsert). `lib/telegram-webhook.ts` har nu et activity-flow før `handleCapture`: regex `ACTIVITY_TRIGGER` pre-filtrerer (sparer Haiku-kald), `detectActivityStart` lader Haiku returnere `{isActivityStart, activityName}`, `handleActivityStart` indsætter eventuel pending som kalenderbegivenhed (`insertEvent` med pending.started_at som start og nu som slut, CPH-timezone) og upserter den nye pending. Bekræftelse på Telegram: `✅ Indsat: X (12:24–14:43). Nu i gang: Y` (kolon-tidsformat via `fmtTimeColonCph` for at matche spec). Hvis kalender fejler bevares pending; hvis ingen pending findes gemmes bare den nye + sendes `▶️ Startet: Y (HH:MM)`. Trigger-fraser: "starter på", "starter med", "går i gang med", "begynder", "skifter til", "påbegynder", "tager fat på". Aktivitets-start gemmes IKKE som capture (det er en kommando, ikke et minde). HÆRDET 2026-06-02 (se takeover-check): regex-bug (`\b` matcher ikke æ/ø/å) fiksede så "starter på"/"tager fat på" faktisk virker; atomicitet (`deletePendingActivity` ved upsert-fejl efter insert); varigheds-cap (`MAX_ACTIVITY_HOURS`, default 16t); `msg.date` som tidsstempel; event-`description`-tag; `hourCycle:'h23'`; migration ryddet (redundant index væk, RLS-kommentar). STOP/AFSLUT-kommando tilføjet 2026-06-02 (`ACTIVITY_STOP_TRIGGER` + `detectActivityStop` + `handleActivityStop`): fraser som "slutter"/"stopper"/"afslutter"/"færdig"/"holder pause"/"done" lukker den aktive aktivitet (skriver den i Google Calendar fra started_at til nu) UDEN at starte en ny; bare "stop" forbliver veto-ord. Ingen ny migration (bruger eksisterende `pending_activity`).
- Dashboard v2 led 2 (balance-view): `/balance` er ny server component. `lib/balance.ts` er TS-port af `scripts/balance.mjs`: henter events via `getEvents(from, to)`, sender til Claude Sonnet med samme persona-prompt som CLI'en, parser kategorier (JSON-array) + rapport adskilt af `---`. Timer pr. kategori regnes præcist i koden, ikke af LLM. Returnerer `BalanceReport` med totals, eventCount, trackedHours, totalHours og rapport-markdown. Cachet via `unstable_cache` (`['balance-v1']`, revalidate 3600s, per `days`-værdi). `app/balance/page.tsx` viser titel + periode-vælger (7/14/30 dage via `?days=`), Suspense-boundary rundt om `BalanceContent`, kategori-bars med farve-mapping pr. kategori, og rapport som whitespace-pre-wrap. Loading-fallback forklarer Sonnet-ventetiden. Top-nav i `app/layout.tsx` har nu "Balance" mellem "I dag" og "Captures". `npx tsc --noEmit` ren. ESLint på rørte filer ren.
- scripts/: load-env, test-db, test-keys, list-models, telegram-poll, telegram-webhook, classify, transcribe, reclassify, show-captures, calendar, balance, calendar-write, propose, watch-actions, show-actions, embed, embed-captures, ask. Kør som `node scripts/<navn>.mjs`.
- lib/: `supabase.ts` (service_role, `server-only`, til interne flows uden bruger-session), `supabase-server.ts` (anon + cookies, til server components/actions/handlers), `supabase-browser.ts` (anon, til client components), `format.ts` (client-sikker datoformat), `ask-types.ts` (client-sikre ask-typer), `memory.ts` (embeddings + idempotent storeChunk), `ask.ts` (ask-funktion til dashboardet, TS-port af scripts/ask.mjs), `balance.ts` (balance-rapport til /balance, TS-port af scripts/balance.mjs, unstable_cache 1 time), `capture.ts` (delt classify + saveCapture, bruges af både Telegram-webhook og dashboard-form), `telegram-webhook.ts` (webhook-flow), `cron.ts` (CRON_SECRET-auth + lock), `telegram.ts` (server-side Telegram send), `calendar.ts` (server-side Google Calendar read/write), `actions-runner.ts` (due actions), `proactive.ts` (ugentligt mønster-flag).
- Portable kontekst (arbejdsform, persona, profil, faldgruber): ligger i `AGENTS.md`. Læses af Claude Code (via `@AGENTS.md`-import i `CLAUDE.md`) OG andre værktøjer (Codex, Cursor osv.). Skifter du værktøj: bed det nye læse `STATUS.md` + `AGENTS.md` først. `README.md` er nu en kort quickstart og arkitekturkort.

## Seneste takeover-check
- Codex 2026-05-26: læste repoets docs, app-routes, auth/proxy-flow, Supabase-klienter, scripts og migrations i bredt review.
- Kørte `npx tsc --noEmit` og `npm run lint`. Begge er rene.
- Dokumentation opdateret for Claude-overdragelse: `README.md` erstattet med projekt-quickstart, `AGENTS.md` fik overdragelsesrutine, denne fil fik sanity-check.
- Codex fortsatte samme dag med Fase 7 led 2: kodede `/api/telegram`, idempotens-migration, webhook-admin script og proxy-fix. Kørte `npx tsc --noEmit`, `npm run lint` og `npm run build` efter ændringer. Build er ren, når Google Fonts må fetches via netværk. Smoke-test: `POST /api/telegram` rammer webhook-routen og returnerer JSON, ikke `/login`; `/captures` returnerer stadig 307 til `/login` uden session. Den kørende dev-server returnerede `TELEGRAM_WEBHOOK_SECRET mangler i env`, så reelt webhook-kald kræver at secreten sættes i lokal/Vercel env.
- Codex fortsatte med Fase 7 led 3: kodede cron-routes, DB-lock migration, proaktive briefings og `vercel.json`. Kørte `npx tsc --noEmit`, `npm run lint`, `npm run build` (ren med netværk til Google Fonts). Smoke-test på port 3002: `/api/cron/actions` og `/api/cron/morning` returnerer JSON 500 med `CRON_SECRET mangler i env`, ikke `/login`; `/captures` returnerer stadig 307 til `/login`. En stale ignoreret `.next/types/cache-life.d 2.ts` build-cache dublet blev slettet for at få typecheck til at læse den reelle kode.
- Codex 2026-05-27: Vercel login via Google virkede trods en grim browser-404. CLI var logget ind som `gustav-bondebjerg-smith`. CLI havde nykonto-bugs omkring personal scope/default team, så team + projekt blev oprettet via Vercel API. `.vercel/project.json` blev skrevet lokalt og er gitignored. `TELEGRAM_WEBHOOK_SECRET` og `CRON_SECRET` blev genereret i `.env.local` fordi de manglede/tomme. Vercel env vars blev sat via API. Production deploy: `https://gustav-os.vercel.app`. Inspect: `https://vercel.com/gustav-os-team/gustav-os/EVXDDpEkxMWhcuinqh2211cHNwbY`.
- Supabase migrations blev først forsøgt i forkert/gammel SQL-tab, hvilket gav irrelevante fejl (`active`/`schedule`). De blev derefter kørt manuelt korrekt i projektet `dxowfjyigfrhyaixyonj`. Schema-cache blev reloadet med `notify pgrst, 'reload schema';`.
- Telegram webhook blev aktiveret efter migrations. Før migration/schema-cache var klar, gav fake update 500 på manglende `telegram_updates`; webhook blev derfor midlertidigt slettet for at undgå rigtige Telegram-fejl. Efter korrekt migrations gav fake update `200 {"ok":true,"accepted":true}`, og webhook blev efterladt aktiv.
- Rigtig Telegram-test 2026-05-27 lykkedes: webhook havde `pending_update_count: 0`, ny capture blev gemt fra production webhook, og aftale-beskeden oprettede en `proposed` calendar action med Telegram-veto.
- Codex 2026-05-31: byggede ekstern cron via GitHub Actions og hjalp Gustav hele vejen gennem GitHub-repo, PAT-token med `workflow` scope, secrets og manual dispatch. Commits på `main`: `d63b901` initial workflow, `9338bd6` offset schedule, `def4c06` eksplicit schedule, `1564ace` smoke-test workflow. Manual dispatch af `Actions cron` returnerede `{"ok":true,"checked_at":"2026-05-31T16:20:13.059Z",...}`. GitHub API viste workflowet som `active`, default branch `main`, og workflowfilen korrekt på GitHub. Alligevel kom der ingen `schedule`-runs efter flere slots. Smoke-workflowet bekræftede samme problem. Claude skal ikke bruge mere tid på at tweake YAML; næste skridt er ekstern scheduler uden for GitHub, helst `cron-job.org`.
- Claude 2026-06-01: LØSTE sub-daglig cron med Supabase pg_cron + pg_net i stedet for GitHub Actions (som aldrig fyrede schedule) og i stedet for cron-job.org (ville lægge CRON_SECRET hos tredjepart). Ny migration `0007_pg_cron_actions.sql`: BLOK 1 gemmer CRON_SECRET i Supabase Vault (`gustav_os_cron_secret`), BLOK 2 schedulerer jobbet `gustav-os-actions` til `*/10 * * * *` der kalder `https://gustav-os.vercel.app/api/cron/actions` med Bearer-header hentet fra Vault. Gustav kørte begge blokke i Supabase SQL Editor; verificeret `{"ok":true}` fra endpointet. Bekræftede at `.env.local`-CRON_SECRET matcher production (rigtigt kald gav `200`, `found:0`). Ryddede OGSÅ op i mappestrukturen (se næste punkt). Committede dette + Codex' ucommittede tids-tracking-arbejde efter Gustavs eksplicitte ja. tsc rent (exit 0).
- Filoprydning 2026-06-01 (Claude, efter Gustavs ja): parent-mappen `../` blev reorganiseret. Slettede `API.txt` (live OpenAI+Anthropic-nøgler i klartekst, begge redundante med `.env.local`, aldrig committet). Oprettede `../reference/` (Roadmap.docx, Life-Audit.docx, Personal-OS-Schematic.pdf, Personal-OS-Build-Cheat-Sheet.pdf, goal-mega-prompt-skabelon.jpg) og `../arkiv/` (goal-time-tracking.md, goal-github-cron.md). Skrev `../INDEX.md` som mappekort. Konsoliderede docs: `AGENTS.md` er nu ENESTE kilde til Gustav-profil + arbejdsmåde + persona + dokument-kort; `CLAUDE.md` er reduceret til tynd pegepind der importerer AGENTS.md (ingen kun-Claude-silo længere); `README.md` cron-sektion opdateret til pg_cron. Stier til docx peger nu på `../reference/`.
- Vercel-GitHub-forbindelse 2026-06-02 (Claude): Gustav forbandt GitHub-repoet til Vercel-projektet (Settings -> Git). FØR dette deployede Vercel kun via manuel `vercel deploy` CLI, så `git push` opdaterede ikke production. NU auto-deployer hvert push til `main`. Verificeret: commit `339adc9` på `main` byggede til `Ready` i production via git-trigger (ikke CLI). Konsekvens for fremtidige agenter: bare `git push` (efter Gustavs ja), så deployer Vercel selv. Tidligere commits skal trigges med et nyt push (fx tom commit `git commit --allow-empty`), da forbindelsen ikke deployer historik retroaktivt.
- Claude 2026-06-02 (kode-review + hærdning af tids-tracking, FØR `0006` køres i prod): Gustav bad om kritisk review inden migrationen. Fundet + fikset i `lib/telegram-webhook.ts` + `0006_pending_activity.sql`:
  - BLOCKER: `ACTIVITY_TRIGGER`-regex brugte `\b`, som i JS ikke matcher æ/ø/å. "starter på" og "tager fat på" (primære fraser) matchede ALDRIG og faldt til capture. Verificeret i node. Fix: Unicode-grænser `(?<![\p{L}\p{N}]) ... (?![\p{L}\p{N}])` + `u`-flag. (Også logget i AGENTS.md faldgruber.)
  - Atomicitet: hvis kalender-insert lykkedes men `upsertPendingActivity` fejlede, blev gammel pending stående -> samme aktivitet kunne indsættes igen (dublet). Fix: ny `deletePendingActivity` rydder pending i den gren.
  - Glemt aktivitet (fx natten over) gav kæmpe falsk kalender-blok. Fix: `MAX_ACTIVITY_MS` (env `MAX_ACTIVITY_HOURS`, default 16t) springer kalender-insert over + advarer; også guard mod varighed <= 0.
  - Tid: bruger nu `msg.date` i stedet for server-`new Date()` -> korrekt aktivitets-grænse + idempotent ved gen-behandling.
  - Tracking-events tagges med `description: 'Tids-tracking via Telegram'` så de kan filtreres i kalenderen.
  - `fmtTimeColonCph`: `hourCycle:'h23'` i stedet for `hour12:false` (robust mod "24:00" på tværs af ICU; testet OK på node v22).
  - Migration `0006`: fjernet redundant index (UNIQUE giver allerede index på chat_id), tilføjet kommentar om at RLS uden policies = bevidst deny-all (service_role bypasser).
  - IKKE ændret (var allerede korrekt): update_id-dedup via `claimTelegramUpdate` i route.ts, og `/`-kommando-afkortning før activity-checket.
  - Checks: `npx tsc --noEmit` rent (exit 0). eslint hang i agent-sandboxen (idle, ~0 CPU, sandbox eller ej) -> kør `npm run lint` i dit eget miljø før push. INTET committet (afventer dit ja). Migration `0006` IKKE kørt i prod endnu.
  - Næste skridt (Gustav): kør `0006` i Supabase SQL Editor (projekt `dxowfjyigfrhyaixyonj`), `npm run lint` + `npm run build`, commit + `git push` (auto-deployer), test "starter på X" -> vent -> "starter på Y" på Telegram (forvent ✅-bekræftelse + event i Google Calendar).
- Claude 2026-06-02 (stop/afslut-kommando + live-verifikation): Gustav bekræftede at hærdningen er LIVE - "starter på testaktivitet" blev forstået (regex-fix virker i prod), og switch-beskeden lukkede aktiviteten + skrev event i Google Calendar som forventet. Det surfacede et hul: ingen måde at AFSLUTTE en aktivitet uden at starte en ny. Tilføjet i `lib/telegram-webhook.ts` (INGEN ny migration - bruger eksisterende `pending_activity`):
  - `ACTIVITY_STOP_TRIGGER` (samme Unicode-grænse-mønster som start pga. æ/ø/å): "slutter(/på/med)", "slut", "stopper(/med)", "afslutter", "holder pause", "tager (en) pause", "pause", "færdig(/med)", "done".
  - `looksLikeActivityStop` (regex-prefilter) + `detectActivityStop` (Haiku ja/nej, ingen navne-udtrækning nødvendig) + `handleActivityStop` (indsætter pending som event fra started_at til nu med SAMME varigheds-guard som skift: spring over hvis <=0 eller >MAX_ACTIVITY_MS; rydder så pending, starter ingen ny). Svar: `⏹️ Stoppet: X (HH:MM–HH:MM). Ingen aktiv aktivitet nu.` eller "Der er ingen aktiv aktivitet at afslutte." hvis ingen pending. Kalender indsættes FØR pending ryddes, så aktiviteten ikke går tabt ved fejl.
  - Routing: stop tjekkes EFTER start (start-fraser rammer aldrig stop-prefilteret) og FØR capture. Start/capture/veto-flowet er URØRT (ingen regression-risiko på det live-verificerede). Bare "stop" forbliver veto (`VETO_WORDS`, tjekkes først i routingen) - derfor "stopper"/"slutter", aldrig bare "stop". Logget i AGENTS.md faldgruber.
  - Checks: lokal `npx tsc --noEmit` OG eslint HANG begge (~11 min på 2,6s CPU-tid, reelt blokeret - se faldgruber; miljø, ikke kode). Faldt tilbage på manuel type-gennemgang (stop-koden spejler 1:1 de allerede type-checkede start-flow-mønstre) + Vercels build som autoritativ type-check. `scripts/telegram-poll.mjs` har IKKE activity-logik (predater featuren, maj-26), så intet at spejle der. Committed (`392a7ce`) + pushed efter Gustavs ja (goal-prompt) -> Vercel auto-deployede.
  - VERIFICERET LIVE 2026-06-02: Gustav testede stop-kommandoen på Telegram efter deploy -> virker som forventet. Dermed passerede Vercel-buildet også (= koden type-checker rent på Vercel, den eneste type-check vi kunne få givet den lokale hang). Stop rydder selv pending, så ingen test-data hænger. Featuren er nu komplet: start/skift + stop.
- Codex 2026-06-02 (Telegram kalender-edit uden veto): implementerede direkte edit-flow for kalender-events i `lib/telegram-webhook.ts` + `updateEvent` PATCH-helper i `lib/calendar.ts`.
  - Triggers: Unicode regex-prefilter for "ret", "ændre", "flyt", "startede X først", "X sluttede", "X gik til", "skubbede X til" før activity/capture-flowet.
  - Haiku intent returnerer `{isEditIntent,eventHint,editType,newTime}` for `end`/`start`/`shift`; Haiku event-match vælger `event_id` blandt events fra startOfTodayCph minus 2 timer til endOfTodayCph.
  - Tidsberegning sker i Europe/Copenhagen: `end` ændrer kun end, `start` ændrer kun start, `shift` bevarer varighed. Succes svarer `✅ Rettet: titel (HH:MM–HH:MM)`. Intet action-forslag og intet veto.
  - Lokal verificering: regex-smoke testede alle fire trigger-eksempler OK; runtime-smoke af tidsberegning gav `end=11:30–13:30`, `start=18:20–19:40`, `shift=14:30–16:00`. `npx tsc --noEmit` og `npm run lint` er begge rene (exit 0). Lokal hang blev løst ved `npm ci` fra lockfilen og fjernelse af macOS extended-attribute metadata fra arbejdsfilerne; en tidligere 312-byte TS-fil tog 16,8s at læse før fix og 2,7ms efter.
- Codex 2026-06-02 (bugfix til kalender-edit): Gustav testede `Ændre personalOS + uni således at den går til nu`, men webhooken faldt tilbage til capture og svarede "Fanget og gemt". Fixet i `lib/telegram-webhook.ts`: edit-prefilteret matcher nu både `gik til` og `går til`; Haiku-prompten kender `"X går til nu"`; `"nu"` normaliseres til Telegram-beskedens timestamp i Europe/Copenhagen og sendes videre som konkret `HH:MM`. Smoke-test: exact phrase matcher edit-prefilteret, gamle end/start/shift-eksempler matcher stadig, og `normalizeEditTime('nu', 2026-06-02T12:34:00+02:00)` giver `12:34`. `npx tsc --noEmit` ren (exit 0) efter sletning af ignored `tsconfig.tsbuildinfo`; `npm run lint` ren (exit 0).
- Claude 2026-06-02 (hverdagssprog + voice i Telegram-routing): Gustav bad om mere "intelligens" - assistenten reagerede kun på faste trigger-ord ("ændre" osv.), og hverdagsagtige voicenotes endte altid som note. To fixes i `lib/telegram-webhook.ts`, INGEN ny migration (bruger eksisterende tabeller/flows):
  - FIX 1 (voice gennem routingen): transskriptionen er flyttet UD af `handleCapture` og OP i `handleTelegramUpdate`. Voicenoten laves til tekst først, og teksten løber gennem præcis samme intent-routing som en skreven besked (edit/activity/stop/capture). Før transskriberede `handleCapture` selv, så voice aldrig nåede routingen og altid blev note. `handleCapture` tager nu `(msg, content, source, opts?)` i stedet for at udlede content/source selv.
  - FIX 2 (hverdagssprog-fallback): ny `triageMessageIntent(text, now)` (Haiku, `TRIAGE_MODEL`). Når INGEN af de hurtige regex-stier rammer, spørger den bredt: `activity_start | activity_stop | calendar_edit | calendar_delete | note`. Routeren ruter så til den rette handler. DOBBELT-GATE: hver handling med en bivirkning bekræftes STADIG af sin egen verificerede detector (`detectActivityStart`/`detectActivityStop`/`detectCalendarEditIntent`) bagefter, så der skal to ja'er til. `calendar_delete` ruter til `handleCapture(..., { forceDelete: true })` og går gennem det eksisterende veto-flow (ikke destruktivt). Alt usikkert + triage-fejl falder til `note` (capture), præcis som før.
  - De live-verificerede regex-stier er bevidst URØRT i adfærd (kun `msg.text` -> den udledte `text`-variabel), så klare kommandoer opfører sig byte-for-byte som før og koster stadig kun ét Haiku-kald. Pris for robustheden: beskeder der IKKE rammer en regex betaler nu ét ekstra triage-Haiku-kald (~$0.0003). Personligt system, lavt volumen -> ubetydeligt.
  - Veto/`/ask`/`/`-kommandoer er nu eksplicit tekst-kun (`source === 'telegram_text'`), så en voicenote aldrig kan vetoe eller ramme en slash-kommando via upålidelig Whisper-transskription. Uændret nettoadfærd (de var allerede `msg.text`-gated).
  - Checks: `npx tsc --noEmit` exit 0 (rent) og `npm run lint` exit 0 (rent). MEN: lokal `tsc` hang igen, og `npm ci` fejlede først med `ENOTEMPTY` på `node_modules/@supabase`. Løst med `rm -rf node_modules` FØR `npm ci` (se faldgrube). Bemærk også: din shell er zsh, så `${PIPESTATUS[0]}` er tom - brug `$?` direkte (eller zsh `$pipestatus[1]`) til at fange exit-koder, ellers ser et fejlet `npm ci` "grønt" ud.
  - INTET committet (afventer dit ja). Næste skridt (Gustav): commit + `git push` (auto-deployer til Vercel), og test på Telegram: voicenote i hverdagssprog "nu kaster jeg mig over anatomien" -> forvent ▶️ aktivitets-start (ikke "Fanget og gemt"); "så holder jeg en pause" mens en aktivitet kører -> forvent ⏹️ stop.
- Claude 2026-06-02 (recall-kategori efter live-test): hverdagssprog + voice blev committet (`2bc0ea9`) og live-testet. Gustav rapporterede 2 huller: (1) "Hvad skulle jeg nå i dag" blev ikke besvaret - naturlige spørgsmål nåede aldrig second brain, kun den literale `/ask`-kommando gjorde. (2) "Tror sgu jeg først går i seng kl 2 i nat" virkede ikke (uafklaret hvad Gustav forventede - afventer svar). Fix for (1) i `lib/telegram-webhook.ts`, INGEN ny migration:
  - Ny triage-kategori `recall`: når Haiku ser et spørgsmål Gustav vil have slået op i hans egne noter/captures/planer ("hvad skulle jeg nå i dag", "hvad havde jeg af opgaver", "hvornår skulle jeg ringe til mor"), ruter routeren til `ask()` (samme second-brain-flow som `/ask`). Voice rammer her også, så talte spørgsmål virker nu uden kommando.
  - `handleAsk` refaktoreret: delt kerne `answerQuestion(chatId, question, label)` bruges af både `/ask` (label `ask`) og recall (label `recall`). `/ask`-adfærd + audit-reasons (`ask_answered`/`ask_failed`) er uændret.
  - DESIGN: recall er READ-ONLY, så den har bevidst INGEN dobbelt-gate (modsat activity/edit/delete). Triagen ruter på egen hånd. Værste fald: en note formuleret som spørgsmål bliver besvaret i stedet for gemt. Triage-prompten skelner derfor eksplicit: recall = Gustav BEDER om info; "husk at ..." (giver info) og retoriske spørgsmål ("hvorfor er jeg så træt") = note. Det bevarer det live-verificerede "Husk at ringe til mor" -> note.
  - Recall-KVALITET er ikke tunet: `ask()` er ren vektor-søgning uden dato-filter, så "i dag"-scoping er ikke garanteret. Routingen er på plads; om "ring til mor" faktisk dukker op afhænger af embeddings. Følg op hvis recall er upræcis (fx dato-filtrering eller flere chunks).
  - Checks: `node_modules/.bin/tsc --noEmit` exit 0 + `npm run lint` exit 0 (begge rene, ingen hang denne gang - node_modules var friskt genbygget fra sidste session).
  - Committet (`d1d8647`) + pushet efter Gustavs ja. Det åbne punkt om "Tror sgu jeg først går i seng kl 2 i nat" blev afklaret og løst i næste entry (søvn-edit).
- Claude 2026-06-02 (søvn-/sengetids-edit): Gustav afklarede #3 - han har en fast daglig søvn-begivenhed (kl 1-9), og "går i seng kl 2 i nat" skal rette dens STARTTID, ikke gemmes som note. Det er altså en kalender-edit der ikke nåede edit-flowet. Fikset i `lib/telegram-webhook.ts` (INGEN ny migration), bevidst UDEN at røre den live-verificerede `CALENDAR_EDIT_TRIGGER`-regex (sengetid ruter via triagen i stedet -> nul regressionsrisiko på de verificerede stier):
  - `triageMessageIntent`: calendar_edit-kategorien nævner nu søvn-begivenheden eksplicit ("tror jeg først går i seng kl 2 i nat" = ret starttid; "jeg står op kl 7" = ret sluttid).
  - `detectCalendarEditIntent` (dobbelt-gate): ny søvn/sengetids-sektion. "går i seng/sover først/i seng kl X" -> editType `start`, eventHint `søvn`. "står op/vågner/sover til kl X" -> editType `end`, eventHint `søvn`. Kun med konkret klokkeslæt; ellers false.
  - `loadTodaysEditableEvents` -> omdøbt `loadEditableEvents`, vinduet udvidet +12t ind i morgen formiddag, så nattens søvn-instans (starter efter midnat = morgendagens dato) er en kandidat. `getEvents` bruger `singleEvents=true` -> gentagne begivenheder udfoldes til daterede instanser; PATCH rammer kun den ene aften, ikke serien.
  - `matchCalendarEditEvent`: får nu den oprindelige besked + nu-tid + en instans-regel, så den vælger NATTENS søvn (førstkommende instans) frem for den der allerede er passeret i dag. "seng"/"sove" matcher titlen "søvn".
  - `computeCalendarEdit` (start-gren): hvis ny starttid lander efter sluttid (sengetid før midnat, fx "kl 23"), prøves dagen før, så søvnblokken forbliver sammenhængende.
  - Sikkerhedsegenskab: hvert Haiku-trin er en gate. Siger triagen ELLER detektoren nej, falder beskeden til note - præcis som før. Worst case = uændret (note), aldrig en forkert kalender-ændring.
  - Checks: `node_modules/.bin/tsc --noEmit` exit 0. `npm run lint` HANG lokalt igen (eslint-proces på 0% CPU, sovende; selv tree-wide `xattr -cr .` hjalp ikke - mistanke om iCloud-synket `Documents`-mappe der staller fil-læsninger, da også `git diff` stallede). Ikke en kodefejl; manuel diff-gennemgang ren. Vercel-build kører lint autoritativt.
  - Committet (`bb84f4d`) + pushet efter Gustavs ja. Næste skridt (Gustav): test på Telegram efter deploy: "tror jeg først går i seng kl 2 i nat" -> forvent ✅ Rettet: søvn (02:00-09:00); "jeg står op kl 7 i morgen" -> forvent søvn-sluttid rettet til 07:00. Bekræft at det er NATTENS søvn der rykkes.
- Claude 2026-06-02 (søvn-edit FIX efter 2. live-test - to bugs fundet via lokal ægte-Haiku-replikering): Gustav testede bb84f4d. Routingen virkede ("går i seng kl 2 i nat" NÅEDE edit-flowet), men svaret var "kan ikke finde søvn i kalenderen i dag" - selvom søvn ER på robottens kalender (Gustav bekræftede: ikke en anden kalender). Diagnosticeret ved at replikere detect+match lokalt mod ÆGTE Haiku + ÆGTE kalender (omgik node_modules-stallet med ren `node:crypto`-JWT i stedet for at importere google-auth-library). To bugs i `lib/telegram-webhook.ts`, INGEN ny migration:
  1. PARSING (direkte årsag): Haiku svarede korrekt JSON i en ```json-fence MEN tilføjede prosa ("Begrundelse: ...") EFTER. Den gamle parse (`raw.replace(/^```/).replace(/```$/)`) fjernede kun en fence i selve enden -> trailing prosa fik `JSON.parse` til at kaste -> `catch` -> null/fallback. Samme skrøbelige mønster var kopieret i ALLE 7 Haiku-parsere. Fix: ny `extractJsonObject<T>()` (scanner balancerede `{}`, respekterer strenge, parser kun første objekt). Brugt i alle 7 (proposeCalendarEvent/Delete, detectCalendarEditIntent, matchCalendarEditEvent, detectActivityStart/Stop, triageMessageIntent).
  2. INSTANS-VALG: Haiku valgte forkert søvn-instans (i morges 03:00, allerede slut) og afviste nattens (01:00) - selv med eksplicitte regler. Fix: stol ikke på Haiku til instans-valg. Haiku matcher kun TITLEN; koden vælger INSTANSEN deterministisk i `matchCalendarEditEvent`: blandt kandidater med samme titel, tag den hvis start er tættest på beskedens `now` (trådet ind via msg.date). Rammer nattens søvn (få timer fremme) frem for morgenens (mange timer tilbage); dagens instans frem for morgendagens for almindelige edits. `loadEditableEvents`-vinduet (+12t ind i morgen) gør nattens instans til kandidat; `singleEvents=true` -> PATCH rammer kun den ene aften.
  - VALIDERET end-to-end lokalt mod ægte Haiku + kalender: "tror jeg først går i seng kl 2 i nat" -> ✅ Rettet: Søvn (02:00-09:00) [nattens instans, vågnetid bevaret]. "jeg står op kl 7" -> Søvn (01:00-07:00). "i nat sover jeg til 10" -> 01:00-10:00. Almindelig edit "flyt cykle til uni til 10:00" -> uændret korrekt (INGEN regression). KENDT EDGE: "står op ... i morgen" -> detect læser "i morgen" som ny/fremtidig og returnerer false (-> note); brug "står op kl 7" uden "i morgen".
  - Checks: end-to-end-valideringen ovenfor er den stærke evidens. tsc lokalt er flaky pga. node_modules-stallet (gav spuriøse "Cannot find module 'next/*'" i GAMLE filer; NUL fejl i den ændrede telegram-webhook.ts). Vercel-build er autoritativ type-check. INTET committet endnu da dette skrives (afventer dit ja).
- Codex 2026-06-02 (Supabase memory MCP til Claude Code + Codex): Implementerede lokal MCP-server oven på Supabase-memory, så Claude Code og Codex kan søge/læse/skrive/opdatere/slette memory via samme værktøj. Nye filer:
  - `supabase/migrations/0008_memory_sources.sql`: ny `memory_sources`, udvidet `memory_chunks`, ny `memory_audit_log`, ny `search_memory_v2(...)`; gammel `search_memory(...)` bevares.
  - `scripts/memory-core.mjs`, `scripts/memory-mcp-server.mjs`, `scripts/memory-import-workspace.mjs`, `scripts/memory-export-backup.mjs`.
  - `.codex/config.toml` (project full-access MCP) og `.mcp.json` (Claude Code project full-access MCP). Global Codex read-only MCP tilføjet i `~/.codex/config.toml`.
  - Dependencies tilføjet: `@modelcontextprotocol/sdk`, `zod`, `mammoth`, `pdf-parse`.
  - Verificeret før migration: MCP tool-list full-access viser 8 tools; read-only viser kun search/list/get/export. `codex mcp list` viser `gustav_memory` enabled. `node scripts/memory-import-workspace.mjs --dry-run --verbose` fandt 103 sources uden writes. DOCX parses; PDF registreres uden tekst i v1.
  - Gustav kørte Supabase CLI-link + migration repair for 0001-0007 + `db push` for 0008. Verificeret bagefter: `npx supabase migration list` viser 0001-0008 både Local og Remote. DB-count: `memory_sources=136`, `memory_chunks=215`, `memory_audit_log=114`. Backup-mappe `../supabase-memory-backup/` har 137 markdown-filer. MCP smoke-test `memory_search("Hvordan skal agenter arbejde med Gustav?")` returnerer relevante hits fra `CLAUDE.md`, `AGENTS.md` og `INDEX.md`.
  - Fix efter smoke-test: MCP-resultater med array-output skulle wrappe `structuredContent` som object (`{ result: [...] }`), ellers fejlede MCP-clienten med "expected record, received array". Rettet i `scripts/memory-mcp-server.mjs`. `npm run lint` exit 0.
- Codex 2026-06-02 (permanent MCP-startfix): Flyttede repo-runtime fra `/Users/gustavbondebjergsmith/Documents/AI assistent/gustav-os` til `/Users/gustavbondebjergsmith/Developer/gustav-os`, så `node_modules` og MCP-start ikke ligger i iCloud-synket `Documents`. Forsøg på kompatibilitets-symlink blev droppet, fordi iCloud auto-omdøbte den til `gustav-os 3`. Opdaterede global Codex MCP (`~/.codex/config.toml`), global Claude Code MCP (`~/.claude.json` via `claude mcp add-json --scope user`), project Codex config (`.codex/config.toml`), project Claude config (`.mcp.json`) og `scripts/memory-core.mjs` `PROJECT_ROOT`. Global Codex og global Claude Code har nu full-access memory i alle projekter. `npm run memory:import` scanner nu både `/Users/gustavbondebjergsmith/Documents/AI assistent` og Developer-repoet separat, hvor repo-filer importeres med stabile `gustav-os/...` source-keys. Verificeret efter flytning: `codex mcp list` peger på Developer-stien, Claude `mcp get gustav_memory` viser `Scope: User config (available in all your projects)` + `Status: ✓ Connected`, MCP smoke-test viser 8 tools og fungerende `memory_search`, import dry-run finder 112 sources fordelt på 11 knowledge-root + 101 repo-runtime, `node_modules/.bin/tsc --noEmit` exit 0, `npm run lint` exit 0.
- Claude 2026-06-03 (strategisk profil + roadmap opdateret): nye reference-filer `Strategisk-profil-v2-2026-06.md` + `Roadmap-v3-2026-06.md` i `../reference/` (+ `INDEX.md` opdateret); `AGENTS.md` profil-sektion ("Strategisk profil (kort)") opdateret; reference-filerne + AGENTS.md importeret til Supabase-memory (`npm run memory:import`).
- Claude 2026-06-03 (knowledge-workspace flyttet ud af iCloud): hele `AI assistent`-mappen (reference, arkiv, INDEX, supabase-memory-backup - 154 filer) flyttet fra `~/Documents/AI assistent` til `/Users/gustavbondebjergsmith/Developer/AI assistent` (uden for iCloud, som gustav-os). `brctl download` materialiserede iCloud-evicted filer først (du gik 1.4M -> 4.0M = nu reelt lokale). Rettet `WORKSPACE_ROOT` + `BACKUP_ROOT` i `scripts/memory-core.mjs` + pegepinde i `AGENTS.md`/`README.md` (AGENTS linje 12's historiske gustav-os-advarsel bevidst urørt). Verificeret: `memory:import:dry` finder stadig 13 workspace-sources på den nye sti (114 total). NB: åbn Claude Code/Codex fra den NYE sti fremover - de laver en frisk projekt-indgang første gang (gen-godkend evt. tilladelser). Claude-memory kopieret til ny projekt-nøgle. `~/.claude.json` + `~/.codex/config.toml` projekt-indgange på gammel sti er IKKE rørt (live-state), så de self-healer ved næste opstart fra ny sti.
- Claude 2026-06-03 (tool-calling router committet, bag `USE_AGENT_ROUTER`-flag): En tidligere session byggede en omskrivning af Telegram-routingen men committede INTET. Jeg verificerede, checkede og committede den. HVAD: ét Sonnet-kald (`claude-sonnet-4-6`) med 7 tools (create_event/edit_event/delete_event/start_activity/stop_activity/search_memory/save_note) erstatter regex+triage+double-gate-cascaden. Routeren UDFØRER intet selv - `routeMessage(text, now)` returnerer kun valget (`RouterResult`: tool/ask/none); `dispatchViaAgent` i webhooken kobler valget til de EKSISTERENDE handlers, så veto/kalender-logik genbruges uændret. Tvivl -> afklarende spørgsmål (kind `ask`), aldrig tavs note.
  - Nye/ændrede filer: NY `lib/agent-router.ts` (server-only, `routeMessage` + 7 tools). MOD `lib/telegram-webhook.ts` (+119/-0, rent additivt: `handleAgentCreate` + `dispatchViaAgent` + flag-gate i `handleTelegramUpdate`, cascaden byte-for-byte urørt). NY `scripts/agent-router.mjs` (CLI), `scripts/replay.mjs` (eval-harness), `scripts/export-golden-set.mjs` (træk fra Supabase). `golden-set-labeling.csv` + `golden-set-raw.json` gitignored (data, ikke kode).
  - Arkitektur: flag-gated. Veto/`/ask`/`/`-slash tjekkes FØR flaget (ens i begge tilstande). Med `USE_AGENT_ROUTER=1`: `dispatchViaAgent` erstatter hele cascaden og ruter tool-valget: create_event->`handleAgentCreate` (action+veto), edit_event->`handleCalendarEdit`, delete_event->`handleCapture` forceDelete (eksisterende slet+veto-flow), start/stop_activity->activity-handlerne, search_memory->`answerQuestion` recall, save_note->`handleCapture`. Uden flaget: cascaden kører uændret -> deploy er sikkert i sig selv.
  - Validering (golden-set, 25 rigtige beskeder): cascaden ~40% misroutes (alle samme fejl: kommando tavst gemt som note); routeren 23/25 korrekt (92%), 0 forkert, 2 afklarende spørgsmål (begge reelt tvetydige), 10/10 gamle misroutes genvundet. Reproducér: `node scripts/replay.mjs`.
  - Checks (denne session, alle RENE og INGEN hang - bekræfter at Developer-flytningen fiksede iCloud-stallet): `node_modules/.bin/tsc --noEmit` exit 0, `npm run lint` exit 0 (også eksplicit på alle 4 nye filer), `npm run build` rent (fuld route-tabel). Brug `node_modules/.bin/tsc`, ikke `npx tsc` (henter forkert pakke); zsh `$?`, ikke PIPESTATUS.
  - Committet (`d850f0f`, 6 filer, 723 insertions) + pushet til main efter Gustavs ja -> Vercel auto-deployer. `USE_AGENT_ROUTER=1` sat i `.env.local` (kun lokal effekt; webhooken peger på prod, så ægte aktivering = Vercel-env nedenfor).
  - AKTIVERING PENDING (Gustav): sæt `USE_AGENT_ROUTER=1` i Vercel prod env (Project gustav-os -> Settings -> Environment Variables, Production) + redeploy. Indtil da kører cascaden i prod. Derefter live-test-tjekliste på Telegram (opret/søvn-edit/slet/start+stop/opslag/note + bevidst tvetydig "slet aftalen" alene -> forvent et SPØRGSMÅL, ikke en tavs note). Rul tilbage: fjern env-var (eller sæt != 1) -> cascaden overtager øjeblikkeligt.
  - OPFØLGNING (separat commit, FØRST når routeren har kørt rent et par dage på Gustavs brug): slet cascaden - regex-prefiltre (`ACTIVITY_TRIGGER`, `ACTIVITY_STOP_TRIGGER`, `CALENDAR_EDIT_TRIGGER`, `DELETE_INTENT`) + de 7 Haiku-funktioner (`detectActivityStart`/`Stop`, `detectCalendarEditIntent`, `proposeCalendarEvent`/`Delete`, `matchCalendarEditEvent`, `triageMessageIntent`) + cascade-blokken i `handleTelegramUpdate`. Behold eksekverings-handlerne (`handleCalendarEdit`, `handleActivityStart`/`Stop`, `handleCapture`, `answerQuestion`) og fjern flaget, så routeren bliver eneste sti.
- Claude 2026-06-03 (samtale-hukommelse til routeren, efter 1. live-test): Gustav live-testede routeren i prod (aktivering BEKRÆFTET live: `telegram_updates` viste `agent_asked` på alle 3 aftenbeskeder kl 19:19-19:20). Symptom: han forsøgte at chatte frem-og-tilbage, men botten stillede spørgsmål i en løkke og fuldførte aldrig handlingen (en PersonalOS-begivenhed blev aldrig oprettet trods 3 ture). DIAGNOSE: routeren var HUKOMMELSESLØS - `routeMessage` fik kun den ene aktuelle besked, aldrig samtalen, så et svar på routerens eget spørgsmål ankom kontekstløst og blev vurderet forfra (loop). Selve routeren var sund (8/8 korrekt på replay, inkl. de 4 cascade-misroutes fra historikken).
  - FIX (4 filer + 1 migration): `lib/agent-router.ts` - `routeMessage(input, now)` tager nu `string | RouterTurn[]` (hele samtalen, ikke kun én besked). Tre nye system-prompt-regler: brug HELE samtalen til at fuldføre (spørg ikke igen om noget allerede oplyst); forbud mod tankestreger (Gustavs regel - Sonnet brød den i et afklarende spørgsmål); fortidig-men-igangværende aktivitet -> `create_event` fra start til nu (`start_activity` er nu-kun). `lib/telegram-webhook.ts` - tre bløde-fejlende helpers (`load/save/clearClarification`) mod ny tabel; `dispatchViaAgent` henter åben afklaring, sender samtalen med til routeren, gemmer ved `ask` (inkl. spørgsmålet), rydder ved enhver handling. Mangler tabellen/DB'en driller -> statsløs fallback (routeren virker præcis som før hukommelsen). `scripts/agent-router.mjs` spejlet + `--convo` til flertur-test. NY `scripts/inspect-recent.mjs` (read-only diagnose: routing-udfald + tekst for sidste N beskeder).
  - Migration `0009_pending_clarification.sql`: lille tabel (`chat_id` unique, `messages` jsonb, `updated_at`), samme RLS-mønster som `pending_activity` (enabled, ingen policies, service_role bypasser). Vindue 5 min (`CLARIFY_WINDOW_MINUTES`), cap 8 ture (`CLARIFY_MAX_TURNS`). ANVENDT i prod via `npx supabase db push --linked` (kun 0009 var ny; DB-password cachet i Keychain fra linket 2. juni). Verificeret: service_role kan SELECT'e tabellen (0 rækker, ingen PostgREST schema-cache-fejl).
  - Verificeret (ægte router-kald via `--convo`, 5/5): den præcise blindgyde (ét-ords "PersonalOS"-svar i kontekst) fuldfører nu `create_event` PersonalOS 16->nu; tvetydig "Slet aftalen" alene spørger stadig (sikkerhed bevaret); slet-svar i kontekst rammer `delete_event`; fortidig-igangværende tur 1 one-shotter nu. Checks: `node_modules/.bin/tsc --noEmit` exit 0, `npm run lint` exit 0, `npm run build` rent.
  - Committet (`62d22d8`) + pushet til main efter Gustavs ja -> Vercel auto-deployer (deployment Ready bekræftet). Cascade-deletion (forrige post) er FORTSAT udskudt til routeren har kørt rent et par dage med hukommelsen.
  - LIVE-BEKRÆFTET 2026-06-03 (Gustav gentog testen i prod): (1) "startede arbejde på PersonalOS kl 16, stadig i gang" oprettede en begivenhed 16->nu; (2) "Slet aftalen" -> svar "spise med skat kl 19" gav et slet-forslag. Begge fuldførte, ingen løkke. Hukommelsen virker end-to-end gennem webhooken.
- Claude 2026-06-03 (lærende fakta-lag - routeren kan nu lære i runtime): Lukkede hullet hvor hver adfærds-korrektion krævede en kode-edit i den hardcodede router-prompt. Routeren kan nu GEMME et faktum/præference (save_memory), og det globale fakta-lag lægges cache-stabilt ind i dens system-prompt, så en korrektion ændrer adfærd uden kode. Bygget som et NYT lille lag VED SIDEN AF memory_sources (dok-RAG), ikke foldet ind: én række = ét faktum, overskrevet på (scope, key). Rent additivt, arver USE_AGENT_ROUTER-flaget.
  - Nye/ændrede filer: NY `supabase/migrations/0010_memory_facts.sql` (tabel `memory_facts`, HNSW cosine-indeks, `match_memory_facts` RPC, genbruger `set_memory_updated_at`-trigger fra 0008, RLS uden policies). NY `lib/memory-facts.ts` (`saveMemory`/`recallGlobal`/`recallProject`/`recallForScope`/`formatGlobalForPrompt`, genbruger `embedText` fra lib/memory.ts - INGEN ny embed-helper). MOD `lib/agent-router.ts` (save_memory-tool; `routeMessage(input, now, globalFacts='')`; system splittet i stabilt prefiks + volatil tids-hale med `cache_control` på prefikset -> prompt-cache hit). MOD `lib/telegram-webhook.ts` (dispatchViaAgent: recallGlobal -> formatGlobalForPrompt -> routeMessage; ny `case 'save_memory'` -> saveMemory, svarer "🧠 Husket: ...", falder blødt til capture ved fejl). NY `scripts/memory-facts.mjs` (--list/--save/--consolidate, genbruger scripts/embed.mjs). MOD `scripts/agent-router.mjs` (spejlet, så replay tester prod-routeren).
  - Beslutninger (afviger bevidst fra handoff'en): tabel `memory_facts` + RPC `match_memory_facts` (ikke bare `memory`/`match_memory`) for ikke at kollidere med memory_sources/chunks/audit_log. Env-navn `NEXT_PUBLIC_SUPABASE_URL` (handoff'ens `SUPABASE_URL` findes ikke i repoet). `embed()` blev IKKE bygget igen - `embedText` fandtes allerede. v1: routeren skriver KUN scope 'global'; projekt-scope + semantisk søgning findes i skema/lib/RPC men wires ikke i Telegram-routeren endnu (search_memory dækker dok-opslag). Threshold ét sted: `FACTS_TOKEN_THRESHOLD=6000`.
  - Sikkerhed: alt nyt ligger i dispatchViaAgent bag det allerede-live USE_AGENT_ROUTER-flag. recallGlobal + save_memory fejler blødt (manglende tabel -> tom fakta-streng / fald til note), så deploy FØR migration er sikkert: routeren opfører sig præcis som i dag indtil 0010 er kørt.
  - Checks (alle RENE; Bash-klassifikatoren var nede et stykke tid men kom tilbage, ingen tsc/eslint-hang): `node_modules/.bin/tsc --noEmit` exit 0, `npm run lint` exit 0. Live router-kald: `node scripts/agent-router.mjs "hold op med at foreslå træning om morgenen, jeg træner om aftenen"` -> save_memory {type:feedback, key:traening-tidspunkt, ...}. Det BEKRÆFTER også at API'et accepterer cache_control-blokkene (ingen 400 = ingen risiko for at routeren falder til note i prod). `node scripts/replay.mjs`: 22/25 korrekt, 0 FORKERT, 3 afklarende (alle på reelt tvetydige beskeder); save_memory stjal ingen golden-case. 0-forkert-invarianten holder = ingen regression.
  - IKKE testet end-to-end mod DB endnu: migration `0010` er IKKE kørt, så save->række->recall->injektion er verificeret-by-construction (typecheck + kode-review), ikke live. INTET committet (afventer dit ja).
  - Næste skridt (Gustav): (1) kør `supabase/migrations/0010_memory_facts.sql` i Supabase SQL Editor (projekt dxowfjyigfrhyaixyonj). (2) Verificér lokalt: `node scripts/memory-facts.mjs --save --type feedback --key traening-tidspunkt --content "Træner om aftenen, ikke morgen" --why "Gustav rettede det"` derefter `node scripts/memory-facts.mjs --list`. (3) Commit + `git push` (auto-deployer; flaget er allerede live i prod). (4) Live-test på Telegram: send "hold op med at foreslå træning om morgenen, jeg træner om aftenen" -> forvent "🧠 Husket: ..."; send derefter en relateret besked -> adfærd afspejler faktummet uden kode-edit.
- Claude 2026-06-03 (fakta-lag eksponeret som MCP-tools): Så Claude Code og Codex kan læse/skrive de SAMME fakta som Telegram-routeren. Trak den delte kerne ud i NY `scripts/memory-facts-core.mjs` (`saveMemory`/`recallGlobal`/`recallProject`/`listFacts`/`consolidateFacts`, genbruger embed.mjs); CLI'en (`scripts/memory-facts.mjs`) er nu en tynd skal oven på den, og MCP-serveren bruger samme kerne (DRY, ingen duplikeret fakta-logik).
  - Nye MCP-tools i `scripts/memory-mcp-server.mjs`: `memory_fact_recall_global`, `memory_fact_recall_project`, `memory_fact_list` (read-only, altid med) + `memory_fact_save` (kun ved `MEMORY_MCP_ACCESS=full`). Arbejdsdeling: Telegram-routeren skriver scope 'global' (fakta om Gustav); Claude Code/Codex kan skrive projekt-scopede fakta (scope = projekt-slug) om den repo de arbejder i. Begge læser via recall. Det er FØRSTE reelle brug af projekt-scope (routeren bruger kun global).
  - Checks: `node --check` på alle tre scripts OK, `npm run lint` exit 0. Import-smoke af kernen: alle 7 exports til stede + DB-vejen kører (recallGlobal gav forventet "table memory_facts not found" indtil 0010 køres). MCP-tools er ikke live-listet endnu (kræver server-genstart).
  - Næste skridt (Gustav): efter migration 0010 + commit, genstart MCP-serveren (luk/genåbn Claude Code/Codex, eller kør `MEMORY_MCP_ACCESS=full npm run memory:mcp` manuelt) så de nye tools dukker op. Global config (read-only) ser kun recall/list; full-access ser også save. `.mcp.json` behøver INGEN ændring - samme serverfil, bare flere tools.
- Claude 2026-06-03 (DEPLOYET + migration kørt, samme session): Gustav gav grønt lys. Committet `2ca456c` (10 filer) + pushet til main -> Vercel auto-deployer. Migration `0010` ANVENDT i prod via `npx supabase db push --linked --yes`. Verificeret mod prod: recallGlobal + recallProject (embed + match_memory_facts RPC) svarer 0 rækker uden fejl mod den tomme tabel = skema, HNSW-indeks, RPC og embed-vej virker. Fakta-laget er dermed LIVE (USE_AGENT_ROUTER var allerede sat i prod), så save_memory + global-fakta-injektion virker når Vercel-deployet er færdigt.
  - Tilladelses-note: første `db push`-forsøg blev blokeret af auto-mode-klassifikatoren (piped auto-confirm + prod-DB-ændring uden klar tilladelse). Det rene `--yes`-kald lykkedes efter eksplicit ja. Vil du lade agenten køre prod-migrations uden per-gang-vurdering fremover, tilføj en allow-regel i `.claude/settings.local.json`: `{ "permissions": { "allow": ["Bash(npx supabase db push:*)"] } }`. Bevidst IKKE tilføjet nu, så prod-DB-skrivning forbliver et aktivt valg.
  - Næste skridt (Gustav): (1) vent på Vercel-deploy (~1-7 min). (2) Live-test på Telegram: send en korrektion -> forvent "🧠 Husket: ..."; send en relateret besked -> adfærd afspejler faktummet. (3) Genstart Claude Code/Codex så de nye MCP fakta-tools (`memory_fact_*`) dukker op.
- Claude 2026-06-03 (FIX: lærte fakta nåede ikke SVARENE, kun routeren): Gustav live-testede og fandt at "angiv ikke kilder" ikke virkede - botten viste stadig kilde-blokken. DIAGNOSE: fakta-injektionen ramte kun routerens prompt (tool-valg). Selve svaret kommer fra `ask()`/`askClaude` (lib/ask.ts), som havde sin EGEN hardcodede prompt (sagde endda "Citer kilderne") og aldrig så de lærte fakta. Plus: "Kilder:"-blokken blev bygget i KODE (`formatSources` i `answerQuestion`), så ingen prompt-fakta kunne fjerne den.
  - FIX: `ask(question, globalFacts)` injicerer globale fakta i answer-prompten; `answerQuestion` loader recallGlobal og lader MODELLEN eje kilde-angivelsen (kode-genereret `formatSources` + `Chunk`/`fmtDate`-imports fjernet). Default: kort kilde-linje fra modellen; lært "angiv ikke kilder" -> modellen dropper kilder helt. Verificeret mod API: MED fakta -> "Du er 22 år." (ingen kilder/numre); UDEN fakta -> "Du er 22 år [1]." Dashboard-ask uændret (kaldes uden fakta). Checks: tsc 0, lint 0. Committet `9731cca` + pushet.
  - GENEREL LÆRING (vigtig for fremtidige fakta): lærte fakta skal nå ALLE bruger-vendte LLM-kald, ikke kun routeren. De rammer nu: (1) routeren (tool-valg) og (2) recall/ask-svar. De rammer IKKE endnu: proaktive briefinger (`lib/proactive.ts` - morgenbrief/aften/mønstre). Vil du have briefinger til at respektere fakta, er det samme mønster: recallGlobal -> formatGlobalForPrompt -> ind i proactive-prompten. Og: rent KODE-genereret output (som den gamle kilde-blok) kan ikke ændres af en fri-tekst-fakta uden at koden gøres fakta-bevidst.
  - Tilladelses-regel sat: `.claude/settings.local.json` (gitignored) tillader `supabase db push`, så agenten kan køre prod-migrations uden per-gang-blokering. Slet filen for at trække tilladelsen tilbage.
- Claude 2026-06-03 (morgen- + aftenbrief afskaffet): Gustav: "De har ingen funktion lige nu." Fjernet helt. HVAD: `lib/proactive.ts` reduceret til kun `buildPatternFlag` (det ugentlige mønster-flag) - `buildMorningBriefing`, `buildEveningReflection` og hjælperne `classifyDay`/`DayShape` slettet; `BriefingKind` er nu kun `'patterns'`; `runProactiveBriefing` simplificeret (72t-dedup + mønster-flag). Cron-ruterne `app/api/cron/morning` + `app/api/cron/evening` slettet, og deres to entries fjernet fra `vercel.json` (kun `actions` daglig + `patterns` ugentlig tilbage). Docs (README + denne fil) opdateret. BEHOLDT bevidst: mønster-flag (søndag kl 11, fyrer kun ved konkret signal) og `/api/cron/actions`. Ingen DB-migration (historiske `proactive_morning`/`proactive_evening` rækker i `audit_log` urørt - harmløs historik). Checks: `node_modules/.bin/tsc --noEmit` exit 0 (efter `rm -rf .next` - stale Next-validator pegede på de slettede ruter), `npm run lint` exit 0. INTET committet (afventer dit ja).

## Sådan bruger du det nu
- Capture (tekst + voice): `node scripts/telegram-poll.mjs` (Ctrl+C stopper, `--once` for én runde).
- Auto-skriv aftaler: kør polleren OG `node scripts/watch-actions.mjs` parallelt i to terminaler. Aftale-beskeder får et Telegram-forslag, og hvis du intet siger inden 10 min, skrives det i kalenderen. Veto med "nej". Hvis watcheren ikke kører, ophobes forslag som proposed og udføres næste gang du starter den.
- Se dine captures: `node scripts/show-captures.mjs`.
- Se dine actions (proposed/executed/vetoed/failed): `node scripts/show-actions.mjs`.
- Spørg din second brain: `node scripts/ask.mjs "spørgsmål"`, send `/ask <spørgsmål>` på Telegram, eller spørg bare i hverdagssprog/voice ("hvad skulle jeg nå i dag") - triagen ruter spørgsmål til samme flow uden kommando.
- Backfill embeddings hvis polleren har været nede: `node scripts/embed-captures.mjs` (idempotent, kun nye behandles).
- Supabase memory MCP (NYT): kør fra `/Users/gustavbondebjergsmith/Developer/gustav-os`. Migration `0008` er kørt. Brug `npm run memory:import:dry`, `npm run memory:import`, `npm run memory:export`. Default-import scanner både `/Users/gustavbondebjergsmith/Developer/AI assistent` og Developer-repoet. Manuel MCP-start: `MEMORY_MCP_ACCESS=full npm run memory:mcp`. Backup ligger i `/Users/gustavbondebjergsmith/Developer/AI assistent/supabase-memory-backup/` og er ikke canonical.
- Balance-rapport: `node scripts/balance.mjs` (eller `--telegram` for at få den på telefonen).
- Efterklassificer ubehandlede captures: `node scripts/reclassify.mjs`.
- Dashboard: `env -u ANTHROPIC_API_KEY npm run dev`, åbn `http://localhost:3000`. Første gang: log ind via magic link (din `ALLOWED_EMAIL`). Magic link havner i indbakken (tjek spam hvis intet kommer; Supabase Free har 3-4 mails/time rate limit). Captures/Actions/Ask i top-nav, "Log ud" øverst til højre.
- Webhook-admin: `node scripts/telegram-webhook.mjs get` viser Telegrams aktuelle webhook. Efter deploy: `node scripts/telegram-webhook.mjs set https://DIT-DOMÆNE/api/telegram`.
- Production URL: `https://gustav-os.vercel.app`.
- Production webhook er aktiv. Send tekst eller voicenote til Telegram-botten for live capture.
- Cron smoke lokalt: `curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/actions`. Kræver `CRON_SECRET` og migration `0005_cron.sql`.
- Sub-daglig action-cron: KØRER via Supabase pg_cron (migration `0007_pg_cron_actions.sql`). Jobbet `gustav-os-actions` kalder `/api/cron/actions` hvert 10. minut, 24/7, uafhængigt af om Macen er tændt. Status tjekkes i SQL Editor: `select * from cron.job where jobname = 'gustav-os-actions';` (active=true) og `select status, start_time from cron.job_run_details where jobid = (select jobid from cron.job where jobname = 'gustav-os-actions') order by start_time desc limit 5;` (succeeded). Endpoint-svar: `select status_code, content from net._http_response order by created desc limit 5;` (200 + {"ok":true}). Stop med `select cron.unschedule('gustav-os-actions');`.
- Polleren + watcheren kan stadig bruges lokalt, men production capture kører nu via webhook. På Vercel Hobby bliver cron dog kun daglig; hyppig action-watch kræver Pro eller ekstern scheduler.
- Tids-tracking: send "starter på X" på Telegram for at starte aktivitet. Send "starter på Y" senere for at lukke X (indsættes i Google Calendar fra X-start til Y-start) og starte Y. Andre start-fraser: "starter med", "går i gang med", "begynder", "skifter til", "påbegynder", "tager fat på". AFSLUT uden at starte en ny: "slutter", "stopper", "afslutter", "færdig", "holder pause", "done" -> lukker X (skriver den i kalenderen) og efterlader ingen aktiv aktivitet. NB: bare "stop" er et veto-ord (ikke en stop-kommando) - brug "stopper"/"slutter". Kræver migration `0006_pending_activity.sql` (stop kræver ingen ny migration).
- Kalender-edit: send fx "unilæsning sluttede 13:30", "jeg startede AI arbejde først 18:20", "flyt træning til 14:30" eller "ændre personalOS + uni således at den går til nu". Botten retter Google Calendar direkte og svarer `✅ Rettet: titel (HH:MM–HH:MM)` uden veto.
- Søvn/sengetid (NYT 2026-06-02): har du en fast daglig søvn-begivenhed, kan du rette den i hverdagssprog. "tror jeg først går i seng kl 2 i nat" retter nattens søvn-STARTtid; "jeg står op kl 7" retter SLUTtiden. Triagen ruter det til edit-flowet (rammer ikke de faste edit-ord), og det rette nat-instans vælges automatisk.
- Hverdagssprog + voice (NYT 2026-06-02): du behøver IKKE de faste trigger-ord længere. Skriver eller siger du noget hverdagsagtigt - fx voicenote "nu kaster jeg mig over anatomien" (start), "så holder jeg en pause" (stop), "træningen rykkede til tre" (edit), "hvad skulle jeg nå i dag" (recall -> slår op i second brain) - klassificerer en Haiku-triage intentionen og ruter til den rette handling. Voicenotes løber gennem samme routing som tekst. Er beskeden tvetydig, gemmes den som note (sikkert default). De faste trigger-ord virker stadig præcis som før og er hurtigst.
- Tool-calling router (NYT 2026-06-03, bag flag): test routerens valg på en enkelt besked uden at røre kalender/DB: `node scripts/agent-router.mjs "din besked"`. Kør hele golden-set'et som regressionstest: `node scripts/replay.mjs`. Routeren er først LIVE i prod når `USE_AGENT_ROUTER=1` er sat i Vercel-env (uden den kører den gamle cascade). Lokalt er flaget allerede sat i `.env.local`.
- Lærende fakta-lag (NYT 2026-06-03, kræver migration `0010`): routeren gemmer varige fakta/præferencer selv via save_memory når du retter den ("hold op med at foreslå morgentræning") og svarer "🧠 Husket: ...". De globale fakta lægges automatisk ind i routerens prompt, så de ændrer adfærd uden kode-edit. Manuelt: `node scripts/memory-facts.mjs --list`, `--save --type feedback --key K --content C [--why W]`, `--consolidate` (foreslår dublet-merges, sletter intet). Claude Code/Codex kan også læse/skrive fakta via MCP-tools (`memory_fact_recall_global`/`_recall_project`/`_list`/`_save`), fx projekt-scopede fakta når de koder. Adskilt fra dok-RAG (memory_sources / `npm run memory:*`): det er importeret korpus, dette er små selvlærte fakta.

## Projektstatus: komplet per original plan
Originalplanens 7 faser + Milestone 0 er alle leveret. Dashboard v2 er en udvidelse af Fase 6 og er også komplet. Adaptiv morgenbrief (sidste post i plan-Fase 7) blev implementeret, men er afskaffet igen 2026-06-03 (ingen reel funktion). Kun det ugentlige mønster-flag er proaktivt tilbage.

Hvad der ikke er per plan, men som ville være naturlige udvidelser:
- Tasks-pipeline: tabellen findes, men der er intet UI eller automatisk task-udledning. Plan-Fase 1 krævede kun at tabellen findes.
- Tasks/daily_logs i embeddings: embed-captures embedder kun raw_captures. Schema er klar.
- Sub-daglig cron: LØST via Supabase pg_cron + pg_net (migration `0007`). Ikke længere en begrænsning. GitHub Actions schedule blev droppet (fyrede aldrig); Vercel Pro er ikke nødvendig.
- Voice-capture i dashboard: pt. kun via Telegram.
- /reflect-side: aften-refleksion er afskaffet 2026-06-03 (sendtes før på Telegram, blev aldrig gemt i daily_logs).

## Faser
0 Life Audit [done] | 0.5 Fundament [done] | 1 Supabase+schema [done] | 2 Capture pipeline [done] | 3 Calendar+balance [done] | 4 Auto-handlinger [done] | 5 Memory/ask [done] | 6 Dashboard [done] | 7a Auth [done] | 7b Webhook [done] | 7c Cron [done] | 7d Deploy [done] | 7e Adaptiv brief [afskaffet 2026-06-03] | v2.1 Root-overblik [done] | v2.2 Balance [done] | v2.3 Filtre [done] | v2.4 Web-capture [done]

## Noter / faldgruber
- VIGTIGT: Claude Code-shellen har en TOM `ANTHROPIC_API_KEY` der skygger for `.env.local`. Kør scripts som `node scripts/x.mjs` (bruger `load-env.mjs`). Kør dev-server som `env -u ANTHROPIC_API_KEY npm run dev` når Anthropic skal virke lokalt.
- Repo-runtime: `gustav-os` bor permanent i `/Users/gustavbondebjergsmith/Developer/gustav-os`. Brug ikke den gamle path i `/Users/gustavbondebjergsmith/Documents/AI assistent/gustav-os`; iCloud auto-omdøber symlinks der. Nye agenter skal arbejde fra Developer-stien.
- npm cache: brug `npm_config_cache=/tmp/gustav-npm-cache` foran npm-installs (root-ejet `~/.npm`). Permanent: `sudo chown -R 501:20 ~/.npm`.
- Deploy: production ligger på `https://gustav-os.vercel.app`. Vercel team er `gustav-os-team`, projekt er `gustav-os`, inspect-link for første prod deploy: `https://vercel.com/gustav-os-team/gustav-os/EVXDDpEkxMWhcuinqh2211cHNwbY`.
- Webhook env: `TELEGRAM_WEBHOOK_SECRET` skal være sat både lokalt og i Vercel. Smoke-test 2026-05-26 viste at den kørende dev-server manglede den, uden at `.env.local` blev læst direkte.
- Webhook idempotens: kør `supabase/migrations/0004_telegram_webhook.sql` før webhook aktiveres. Ellers returnerer `/api/telegram` 500 ved første rigtige update.
- Tids-tracking migration: kør `supabase/migrations/0006_pending_activity.sql` før activity-flowet bruges. Uden tabellen returnerer `pending_activity select`/`upsert` fejl og webhook falder tilbage til normal capture med fejl-besked på Telegram.
- Cron env: `CRON_SECRET` skal være sat i Vercel og lokalt hvis cron-routes skal testes. Vercel sender den automatisk som `Authorization: Bearer <CRON_SECRET>` når env-var findes.
- Cron migration: kør `supabase/migrations/0005_cron.sql` før `/api/cron/actions` eller `scripts/watch-actions.mjs` bruges efter denne ændring. Watcheren skriver nu `actions.processing_started_at`.
- Sub-daglig cron LØST (2026-06-01): Supabase pg_cron + pg_net via migration `0007_pg_cron_actions.sql`. Kør den manuelt i Supabase SQL Editor (samme mønster som øvrige migrations): BLOK 1 gemmer CRON_SECRET i Vault (indsæt nøglen hvor der står PASTE_DIN_CRON_SECRET_HER), BLOK 2 schedulerer jobbet. Idempotent: samme jobnavn opdaterer. Hvis CRON_SECRET roteres, kør BLOK 1 igen med ny nøgle (vault.create_secret fejler hvis navnet findes - brug da `select vault.update_secret((select id from vault.secrets where name='gustav_os_cron_secret'), 'NY_NØGLE');`).
- GitHub Actions cron-status: `.github/workflows/cron-actions.yml` + `schedule-smoke.yml` findes på `main` men `schedule` fyrede aldrig (kun manuel dispatch virker). Erstattet af pg_cron. Filerne kan slettes eller blive liggende som manuel smoke-test; de skader ikke.
- Supabase Auth prod: Site URL skal sættes til `https://gustav-os.vercel.app`; Redirect URL skal inkludere `https://gustav-os.vercel.app/auth/callback` plus lokal `http://localhost:3000/auth/callback` hvis lokal login stadig skal virke.
- Telegram webhook: er aktiv på `https://gustav-os.vercel.app/api/telegram`. Hvis migrations/schema-cache ændres i fremtiden, slet webhook midlertidigt med `node scripts/telegram-webhook.mjs delete`, fix DB, og sæt den igen.
- Vercel Cron timezone: schedules i `vercel.json` er UTC. `0 5 * * *` er derfor ca. 07 dansk sommertid og 06 dansk vintertid.
- Vercel Hobby: officielle Cron-limits tillader kun daglige schedules. `vercel.json` er derfor Hobby-safe. Hvis veto-actions skal udføres få minutter efter deadline, brug Vercel Pro eller ekstern scheduler der kalder `/api/cron/actions` hvert 10. minut med `Authorization`-header. Konkret anbefaling efter 2026-05-31-testen: `cron-job.org`.
- Proxy: `/api/*` skal forblive ekskluderet i `proxy.ts`. API-routes validerer sig selv. Hvis `/api/telegram` begynder at returnere 307 til `/login`, er matcheren blevet ødelagt.
- Whisper: ~95% nøjagtig på dansk. Råtekst gemmes altid, så intet går tabt.
- Google service account: nøgle ligger på én linje i `.env.local` med tekst-`\n`. Fold ud med `.replace(/\\n/g, '\n')` før brug. Læse-scope (`calendar.readonly`) i `calendar.mjs`, skrive-scope (`calendar.events`) i `calendar-write.mjs` - hold dem adskilt.
- audit_log: gemmer kun terminal-states (`applied`/`vetoed`/`failed`). Forslag (proposed actions) bor kun i `actions`-tabellen. Hvis Supabase under setup ikke kender `actions.type` (eller andre kolonner mangler), kør `drop table actions cascade;` og derefter `0002_actions.sql` igen - tabellen er kun forslag, så det er sikkert.
- pgvector ivfflat-index: kræver mere `maintenance_work_mem` end Supabase's default 32MB. Først relevant ved tusinder af chunks. Når du tilføjer det, foranstil med `SET maintenance_work_mem = '128MB';` i samme SQL-blok. Indtil da er sequential scan hurtigt nok.
- PostgREST schema-cache: efter nye funktioner/tabeller skal cachen reloades. Kør `notify pgrst, 'reload schema';` i SQL Editor hvis du får PGRST202 ("Could not find the function...").
- Embed-modellen er `text-embedding-3-small` (1536 dim). Skifter du model, skal `memory_chunks.embedding`-kolonnen recreates med ny dimension OG alle eksisterende rækker re-embeddes. Lås modellen indtil det giver mening. SAMME lås gælder `memory_facts.embedding` (fakta-laget); et modelskift kræver re-embed af BEGGE tabeller.
- Fakta-lag (`memory_facts`, migration `0010`): adskilt fra memory_sources/chunks med vilje (én række = ét faktum, overskrives på (scope, key); ikke chunket). Routeren skriver kun scope 'global'. recallGlobal + save_memory fejler blødt -> hvis `0010` ikke er kørt, opfører routeren sig præcis som før (tom fakta-streng, save_memory falder til note). Global-laget skal holdes lille (load i fuld hver besked); kun store projekt-scopes rammer `FACTS_TOKEN_THRESHOLD` og skifter til vektor-søgning.
- Next.js 16 server actions: filer med `'use server'` i toppen må KUN eksportere async funktioner. Konstanter og type-exports skal ligge i separat fil ellers serialiseres de som Server References og dukker som `undefined` op på klienten.
- Next.js 16 file convention: `middleware.ts` er deprecated, brug `proxy.ts` med eksporteret funktion `proxy()`. Default runtime er Node.js (ikke Edge). Codemod findes: `npx @next/codemod@canary middleware-to-proxy .`.
- Server actions er IKKE separate routes i proxy-chain-pipelinen; de POST'er til den route hvor de bor. En matcher-ændring eller refaktor der flytter en server action kan silently fjerne proxy-coverage. Derfor verificerer `askAction` selv at brugeren er logget ind (`getServerSupabase` + `getUser` + whitelist). Gør det samme i alle fremtidige server actions.
- Auth: brug `getUser()` (round-trip til Supabase) eller `getClaims()` (lokal JWT-validering) til autorisationsbeslutninger. `getSession()` læser kun fra cookies og må IKKE bruges til at gate noget (en angriber kan crafte en cookie).
- Supabase magic link rate limit: Free tier sender 3-4 mails pr. time fra deres SMTP. For test-loops: log ind én gang, brug "Log ud"-knappen, log ind igen - cookies invalideres uden ny mail.
- Service_role-klienten (`lib/supabase.ts`) er nu mærket med `import 'server-only'`. Hvis nogen importerer den til et client component knækker buildet med det samme. Brug `getServerSupabase()` fra `lib/supabase-server.ts` til alt der har med en bruger at gøre.
- Lokal dev-server kan hænge på `○ Compiling proxy ...` flere minutter på cold cache; build-tid var 7 min i samme session. Hvis dev er hængt: `pkill -9 -f next && rm -rf .next` og giv den tid. Husk `npx next telemetry disable` første gang, ellers venter dev på en prompt der aldrig kommer i baggrundsprocesser. Start dev direkte (`env -u ANTHROPIC_API_KEY node node_modules/.bin/next dev`) for at få stdout uden npm-buffer.
- git: Fase 1+2 committet (db1e152). Fase 3 committet (0e5e466). Fase 4 committet (00ad3f3 + polering 02f1736). Fase 5 committet (4ffe089). Fase 6 committet (cf305ba). Fase 7 auth committet (d0cb2bc). Fase 7 webhook+cron committet (f3f83d6). Fase 7 deploy-status committet (014d274). Dashboard v2 led 1 (root-overblik) er endnu ikke committet. GitHub Actions cron-forsøg er committet (`d63b901`, `9338bd6`, `def4c06`, `1564ace`). Claude/Codex committer kun når Gustav beder om det.
