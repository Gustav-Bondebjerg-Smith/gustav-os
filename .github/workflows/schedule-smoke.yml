name: Schedule smoke

on:
  schedule:
    - cron: '2,7,12,17,22,27,32,37,42,47,52,57 * * * *'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Print schedule event
        run: |
          echo "event=${{ github.event_name }}"
          date -u
